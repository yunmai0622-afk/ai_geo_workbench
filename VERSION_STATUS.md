# V1.2 P0 友好客户试跑版冻结状态

当前项目版本已冻结为 **V1.2 P0 友好客户试跑版**。该版本已完成 P0 后端真实主链路验收、P0 前端 `/flow` 操作链路验收，并已通过 `pnpm check`、`pnpm test` 与 `pnpm build`。当前版本可进入 **1 个客户人工陪跑试跑**，试跑主入口为 `/flow`。

| 项目 | 当前状态 |
|---|---|
| 冻结版本 | V1.2 P0 友好客户试跑版 |
| 试跑入口 | `/flow` |
| 试跑方式 | 1 个客户人工陪跑试跑 |
| 已通过验收 | P0 后端真实主链路、P0 前端 `/flow` 操作链路、`pnpm check`、`pnpm test`、`pnpm build` |
| 当前可试跑链路 | AI 诊断 → 3 篇内容生成 → 1 篇内容发布 → 收录监测初始记录 → 客户报告 |

从当前版本开始，项目进入冻结维护状态。后续不允许新增功能、不允许继续优化 UI、不允许扩展模块。仅允许处理真实客户试跑中发现的阻断 bug、明显错误文案，以及经客户反馈确认后的必要小修。

| 后续处理类型 | 是否允许 | 说明 |
|---|---:|---|
| 真实客户试跑中的阻断 bug | 允许 | 仅修复导致主链路无法继续的错误 |
| 明显错误文案 | 允许 | 仅修正错字、误导性状态或明显不准确表述 |
| 客户反馈确认后的必要小修 | 允许 | 必须先确认，不主动扩展范围 |
| 新功能 | 禁止 | 全部进入 V1.3 待评估清单 |
| UI 继续优化 | 禁止 | 不做视觉、布局或交互优化 |
| 模块扩展 | 禁止 | 不新增入口、页面、后台或复杂配置 |

以下能力全部进入 **V1.3 待评估清单**，不得在 V1.2 P0 友好客户试跑版中继续实现或扩展：第三方自动发布、真实外部收录检测、自动抓取、多租户、支付、权限、代理商后台、方案生成。
