# AI GEO 增长工作台 V1.0 可售卖工程 Harness

## 一、V1.0 产品定义

V1.0 产品名：企业 GEO 内容增长工作台。

一句话价值：帮助企业发现 AI 搜索推荐缺口，并持续生成、审查、发布符合 GEO 逻辑的高质量内容资产，提升企业在豆包、DeepSeek、Kimi、腾讯元宝、百度 AI 搜索、知乎直答等 AI 问答入口中的可见度。

V1.0 的工程判断标准不是功能多，而是客户主链路真实、内容增长闭环完整、发布边界清晰、关键产物可复查。每个页面动作都必须对应真实后端 API、真实数据库记录或明确的不可执行阻断原因。

## 二、V1.0 完整客户路径

V1.0 客户路径固定为 10 步：

1. 创建企业 GEO 项目。
2. 完善企业档案和资料来源。
3. 录入 AI 原始回答。
4. 运行 AI 诊断。
5. 生成 GEO 评分和优化任务。
6. 配置内容生产计划：每周篇数、内容类型、目标平台。
7. 生成本周内容选题和文章。
8. 执行 GEO 内容质量审查和反同质化检查。
9. 人工确认后发布到目标平台。
10. 保存发布记录并生成交付报告。

V1.0 不再定义为单次诊断或单篇文章生成工具，而是从诊断缺口到内容生产、质量审查、平台发布、发布记录和交付报告的最小内容增长闭环。

## 三、V1.0 目标 AI 入口

V1.0 主要面向国内 AI 搜索 / AI 问答入口：

1. 豆包
2. DeepSeek
3. Kimi
4. 腾讯元宝
5. 百度 AI 搜索 / 文心一言
6. 通义 / 夸克
7. 知乎直答
8. 微信搜索 / 搜一搜

所有内容策略、平台矩阵和质量审查，都应服务于这些 AI 入口的可检索、可引用、可复查价值。

## 四、V1.0 发布平台矩阵

发布平台不是随意选择，必须按国内 AI 搜索 / AI 助手的收录与引用价值确定。

### A 级：必须优先支持

1. 自有内容站 / 企业官网 GEO 页面
2. 微信公众号草稿箱
3. 知乎文章 / 回答
4. 百家号
5. 头条号

### B 级：可配置，但自动发布可后置

1. 小红书
2. 搜狐号
3. 网易号
4. CSDN / 掘金，面向技术 / SaaS 类客户

## 五、平台能力等级

V1.0 对每个平台必须标注当前能力等级和原因。

1. A 级能力：API 自动发布。平台具备稳定、合规、可授权的开放接口，且系统完成真实授权、发布、失败记录和回查。
2. B 级能力：草稿箱 / 半自动发布。系统可生成平台适配内容并进入草稿或半自动流程，最终发布必须由人工确认。
3. C 级能力：内容适配 + 人工复制发布。系统只提供平台适配文案、标题、摘要、标签和发布注意事项，由交付人员人工复制发布并回填记录。

V1.0 不允许对所有平台强行承诺自动发布。每个平台页面都必须显示当前支持级别、限制原因、是否需要授权、是否需要人工确认。

## 六、V1.0 发布能力边界

V1.0 支持：

1. 平台授权配置。
2. 内容格式适配。
3. 发布前质量审查。
4. 反同质化检查。
5. 人工确认发布。
6. 自动或半自动发布到已授权平台。
7. 发布成功 / 失败记录。
8. 发布失败原因展示。

V1.0 不支持：

1. 无人审核自动发布。
2. 账号矩阵批量发布。
3. 自动养号。
4. 自动绕风控。
5. 自动收录监测。
6. 自动复测 AI 推荐。
7. 多账号轮换。
8. 验证码处理。
9. 模拟登录绕过平台限制。

任何发布能力都必须先通过质量审查、反同质化检查和人工确认。未授权平台不得显示为“可自动发布”。

## 七、GEO 内容质量审查要求

GEO 内容质量审查必须检查：

1. 是否解决诊断缺口。
2. 是否引用企业资料来源。
3. 是否有事实依据。
4. 是否避免编造客户案例。
5. 是否避免过度承诺。
6. 是否和历史内容同质化。
7. 是否适合 AI 引用。
8. 是否结构化清晰。
9. 是否包含品牌实体信息。
10. 是否包含产品 / 服务 / 案例 / FAQ / 对比信息。
11. 是否适合目标发布平台。
12. 是否满足平台基础格式要求。

质量审查结果必须客户可读：展示总分、事实一致性、阻断原因、平台适配问题、反同质化风险和优化建议。不得把 raw JSON、内部表名或技术错误作为主展示。

## 八、反同质化要求

V1.0 必须至少支持：

1. 读取同项目历史文章。
2. 检查标题重复。
3. 检查选题重复。
4. 检查内容结构重复。
5. 检查核心观点重复。
6. 给出差异化角度建议。
7. 同一优化任务下避免连续生成高度相似内容。
8. 同一周内容避免重复覆盖同一问题。

反同质化不等于随机改写。它必须基于诊断缺口、目标平台、企业资料、历史文章和本周内容计划，判断内容是否真正补齐新的 GEO 可引用资产。

## 九、V1.0 状态机

### S0 项目未创建

- 进入条件：用户已登录，但当前没有企业 GEO 项目。
- 完成条件：创建企业项目并写入 `projects`。
- 下一步动作：完善企业档案和资料来源。
- 关键产物：项目 ID、企业名称、行业、官网、目标客户、核心卖点、竞品、关键词。
- 阻断原因：无登录态、数据库不可用、项目必填字段缺失。
- 对应页面：`/enterprise-profile` 或首页轻量创建入口。
- 对应后端 API：`geo.projects.create`、`geo.projects.list`。
- 对应验收方式：`accept:v1:sellable` 和当前 P0-1 脚本参考。

### S1 企业档案和资料来源待完善

- 进入条件：项目已创建，但企业档案、资料来源、客户案例、竞品、合规规则不足。
- 完成条件：企业档案保存成功，至少有可公开、已确认、允许用于生成的资料来源。
- 下一步动作：录入 AI 原始回答。
- 关键产物：`enterprise_geo_profiles`、`geo_asset_sources`、`customer_cases`、`competitor_profiles`、`compliance_rules`。
- 阻断原因：资料来源缺失、真实案例无 `sourceAssetIds`、资料不可公开、资料未人工确认。
- 对应页面：`/enterprise-profile`。
- 对应后端 API：`geo.assetLibrary.upsertProfile`、`geo.assetLibrary.addTextSource`、`geo.assetLibrary.createCustomerCase`、`geo.assetLibrary.createCompetitor`、`geo.assetLibrary.createComplianceRule`、`geo.assetLibrary.summary`。
- 对应验收方式：`corepack pnpm accept:p0:enterprise-profile`。

### S2 AI 回答待录入

- 进入条件：项目和企业资料已具备，尚未录入真实 AI 原始回答。
- 完成条件：至少录入 1 组客户问题和对应 AI 原始回答；V1.0 最小建议为 2 条。
- 下一步动作：运行 AI 诊断。
- 关键产物：`questions`、`ai_responses`。
- 阻断原因：没有项目、问题为空、AI 原始回答为空、样本不是客户真实问题。
- 对应页面：`/ai-diagnosis`。
- 对应后端 API：`geo.questions.batchAddSpecified`、`geo.questions.create`、`geo.aiResponses.create`、`geo.aiResponses.importCsvRows`、`geo.questions.list`、`geo.aiResponses.list`。
- 对应验收方式：`corepack pnpm accept:p0:ai-diagnosis`。

### S3 AI 诊断待运行

- 进入条件：AI 原始回答已录入，尚未生成诊断结果。
- 完成条件：调用真实 LLM 完成 `geo.analysis.run`，并写入诊断结果。
- 下一步动作：生成 GEO 评分和优化任务。
- 关键产物：`analysis_results`。
- 阻断原因：缺少 AI 原始回答、LLM provider 环境变量缺失、OpenAI-compatible endpoint 不可用、LLM 返回非 JSON 或字段不完整。
- 对应页面：`/ai-diagnosis`。
- 对应后端 API：`geo.analysis.run`、`geo.analysis.list`。
- 对应验收方式：`corepack pnpm accept:p0:ai-diagnosis`。

### S4 GEO 评分和优化任务已生成

- 进入条件：`analysis_results` 已生成并可读回。
- 完成条件：完成 GEO 评分计算，并生成至少 1 条优化任务。
- 下一步动作：配置内容生产计划。
- 关键产物：`geo_scores`、`optimization_tasks`。
- 阻断原因：诊断结果为空、关键诊断字段缺失、评分记录未写入、任务为空。
- 对应页面：`/ai-diagnosis`、`/content-generation`。
- 对应后端 API：`geo.scores.calculate`、`geo.scores.latest`、`geo.tasks.generate`、`geo.tasks.list`。
- 对应验收方式：`corepack pnpm accept:p0:ai-diagnosis`、`corepack pnpm accept:p0:content-generation`。

### S5 内容生产计划待配置

- 进入条件：优化任务已生成，但尚未配置本周内容计划。
- 完成条件：配置每周篇数、内容类型、目标平台和优先优化任务。
- 下一步动作：生成本周内容选题。
- 关键产物：内容生产计划记录，至少包含周次、篇数、内容类型、目标平台、关联优化任务。
- 阻断原因：没有优化任务、没有目标平台、每周篇数为空、目标平台能力等级未标注。
- 对应页面：`/content-generation`。
- 对应后端 API：后续 Phase 3 明确或补齐。
- 对应验收方式：后续 Phase 3 页面验收和 `accept:v1:sellable`。

### S6 本周内容待生成

- 进入条件：内容生产计划已配置。
- 完成条件：生成本周内容选题和至少 1 篇文章，且文章绑定优化任务、诊断缺口、目标平台。
- 下一步动作：执行质量审查和反同质化检查。
- 关键产物：`geo_article_topics`、`geo_articles`。
- 阻断原因：缺少客户指定问题、缺少 AI 诊断结果、缺少优化任务、缺少内容计划、缺少目标平台。
- 对应页面：`/content-generation`。
- 对应后端 API：`geo.articles.topics.generate`、`geo.articles.topics.list`、`geo.articles.generate`、`geo.articles.list`。
- 对应验收方式：`corepack pnpm accept:p0:content-generation` 和后续 Phase 3 验收。

### S7 内容质量审查和反同质化待完成

- 进入条件：文章已生成且状态允许质检。
- 完成条件：生成质量评分、事实一致性结果、反同质化结果和平台适配结果。
- 下一步动作：人工确认发布。
- 关键产物：`geo_article_quality_scores`、文章 `factTraceability`、文章 `consistencyCheck`、反同质化检查结果、平台适配检查结果。
- 阻断原因：质量分低于阈值、合规阻断、事实来源缺失、一致性检查不通过、标题/选题/结构/核心观点与历史内容高度重复、目标平台格式不适配。
- 对应页面：`/content-generation`。
- 对应后端 API：`geo.articles.qualityCheck`、`geo.articles.latestQualityScores`，反同质化和平台适配 API 后续 Phase 4 明确或补齐。
- 对应验收方式：`corepack pnpm accept:p0:content-generation` 和后续 Phase 4 验收。

### S8 平台授权和发布目标待确认

- 进入条件：内容质检通过或已明确可进入人工审核。
- 完成条件：目标平台已配置，能力等级 A/B/C 已显示，授权状态和发布方式清楚。
- 下一步动作：人工确认发布。
- 关键产物：平台配置、授权状态、能力等级、发布格式适配结果。
- 阻断原因：平台未配置、平台未授权、能力等级未标注、目标平台只支持人工复制但页面误显示自动发布。
- 对应页面：`/content-publishing` 或后续发布平台矩阵页面。
- 对应后端 API：`geo.assetLibrary.createPublishStrategy`、平台授权配置相关 API，后续 Phase 5 明确或补齐。
- 对应验收方式：后续 Phase 5 页面验收。

### S9 人工确认后发布

- 进入条件：内容质检、反同质化、平台适配通过，目标平台能力边界清楚。
- 完成条件：人工确认后发布到目标平台；A/B 级渠道可自动或半自动发布，C 级渠道生成适配内容并由人工复制发布。
- 下一步动作：保存发布记录。
- 关键产物：发布请求、平台返回结果、人工确认记录、失败原因。
- 阻断原因：无人审核、平台未授权、平台风控、验证码、格式不符合平台要求、API 不可用。
- 对应页面：`/content-publishing`。
- 对应后端 API：`geo.articles.publish` 或后续平台发布 API。
- 对应验收方式：后续 Phase 6 页面验收和 `accept:v1:sellable`。

### S10 发布记录已保存

- 进入条件：发布动作完成或人工复制发布后回填结果。
- 完成条件：保存发布成功 / 失败记录、平台、URL、失败原因、发布时间、是否需要后续人工复查。
- 下一步动作：生成交付报告。
- 关键产物：`geo_publish_records`。
- 阻断原因：发布结果未回填、失败原因为空、平台记录与实际发布不一致。
- 对应页面：`/content-publishing`、`/delivery-reports`。
- 对应后端 API：`geo.articles.publishRecords`、发布记录创建 / 更新 API。
- 对应验收方式：后续 Phase 6 页面验收和 `accept:v1:sellable`。

### S11 报告已生成

- 进入条件：诊断、评分、任务、内容、质检、发布记录均存在。
- 完成条件：报告能引用企业档案、诊断结果、评分、优化任务、内容、质检结果和发布记录。
- 下一步动作：客户交付或进入下一周内容计划。
- 关键产物：`reports`、报告正文、报告 ID、生成时间、总分、核心结论、发布记录摘要。
- 阻断原因：报告为空、报告只引用静态演示数据、缺少发布记录、缺少风险说明、缺少关键产物引用。
- 对应页面：`/delivery-reports`。
- 对应后端 API：`geo.reports.generate`、`geo.reports.latest`。
- 对应验收方式：后续 Phase 7 页面验收和 `accept:v1:sellable`。

### S12 V1.0 交付完成

- 进入条件：项目、档案、资料来源、AI 回答、诊断、评分、任务、内容计划、文章、质检、反同质化、平台发布、发布记录、报告均真实完成。
- 完成条件：所有关键产物可从页面进入，可从数据库复查，可通过 V1.0 验收脚本复跑。
- 下一步动作：客户交付，或进入下一周内容增长计划。
- 关键产物：完整项目 ID、企业档案、资料来源、AI 回答、诊断结果、GEO 评分、优化任务、内容计划、文章、质量评分、反同质化结果、发布记录、报告。
- 阻断原因：任一主链路依赖 mock / 静态数据 / 假按钮，或缺少真实 API 验收。
- 对应页面：`/`、`/flow`、`/enterprise-profile`、`/ai-diagnosis`、`/content-generation`、`/content-publishing`、`/delivery-reports`。
- 对应后端 API：V1.0 范围内全部真实 API。
- 对应验收方式：后续 `corepack pnpm accept:v1:sellable`。

## 十、当前已通过验收

当前已通过的工程验收：

1. `corepack pnpm accept:p0:enterprise-profile`
2. `corepack pnpm accept:p0:ai-diagnosis`
3. `corepack pnpm accept:p0:content-generation`
4. `corepack pnpm check`
5. `corepack pnpm test`
6. `corepack pnpm build`

这些验收说明当前后端真实链路可用，但不代表新的 V1.0 内容增长闭环已经全部完成。新的 V1.0 还必须补齐内容生产计划、反同质化、平台矩阵、人工确认发布、发布记录和报告引用。

## 十一、后续 Phase 执行顺序

后续 Codex 阶段固定如下，未经用户明确允许不得跨 Phase：

### Phase 1：`/enterprise-profile` 页面主链路

- 状态：已完成。
- 目标：项目创建 + 企业档案 + 资料来源录入 + 客户案例 `sourceAssetIds`。
- 完成标准：客户可从页面创建项目，保存企业档案，录入至少一条可公开、已确认、允许用于生成的资料来源，并创建可引用客户案例或明确标注待补充。

### Phase 2：`/ai-diagnosis` 页面主链路

- 状态：已完成。
- 目标：问题录入 + AI 原始回答录入 + 运行诊断 + 查看结果 / 评分 / 任务。
- 完成标准：客户可从页面录入问题和 AI 回答，运行真实 `geo.analysis.run`，查看诊断结果、GEO 评分和优化任务。

### Phase 3：内容生产计划 + 内容生成页面工程化

- 目标：每周篇数、目标平台、内容类型、基于优化任务生成本周内容。
- 完成标准：客户可配置本周内容计划，选择目标平台和内容类型，基于优化任务生成本周选题和文章。
- 禁止扩展：发布执行、自动收录监测、账号矩阵、自动抓取 AI 平台回答。

### Phase 4：GEO 内容质量审查 + 反同质化检查

- 目标：质量评分、事实一致性、反同质化、平台适配检查。
- 完成标准：页面展示质量总分、事实一致性、历史内容重复风险、差异化角度建议、平台格式适配结果和阻断原因。
- 禁止扩展：无人审核自动发布、绕过质量检查、用随机改写冒充反同质化。

### Phase 5：发布平台矩阵 + 授权配置

- 目标：配置自有内容站、微信公众号、知乎、百家号、头条号等目标平台，并标注 A/B/C 能力等级。
- 完成标准：每个平台显示平台等级、能力等级、授权状态、支持原因、限制原因和发布方式。
- 禁止扩展：多账号轮换、自动养号、验证码处理、模拟登录绕过平台限制。

### Phase 6：人工确认后发布 + 发布记录

- 目标：至少打通 1 个 A/B 级发布渠道，支持人工确认后发布，记录成功 / 失败。
- 完成标准：发布动作必须由人工确认触发；发布成功或失败都写入发布记录，并展示失败原因。
- 禁止扩展：无人审核自动发布、自动收录监测、自动复测 AI 推荐。

### Phase 7：交付报告页面工程化

- 目标：报告引用诊断、任务、内容、质检、发布记录。
- 完成标准：报告能展示企业档案、AI 诊断、GEO 评分、优化任务、本周内容、质量审查、发布记录和风险说明。
- 禁止扩展：复杂多报告系统、PPT 自动生成、支付套餐。

### Phase 8：首页 / Flow 产品 Harness

- 目标：展示当前阶段、下一步、阻断原因、关键产物入口。
- 完成标准：首页和 `/flow` 能根据真实数据展示 V1.0 状态机位置、阻断原因和下一步入口。
- 禁止扩展：大屏、运营后台、复杂数据可视化。

### Phase 9：`accept:v1:sellable` 全链路验收脚本

- 目标：验证从建档到发布记录再到报告的最小闭环。
- 完成标准：脚本从零创建项目，走企业档案、资料来源、AI 回答、诊断、评分、任务、内容计划、文章、质检、反同质化、人工确认发布、发布记录、报告，并断言没有触碰 V1.0 禁止范围。
- 禁止扩展：mock 数据、跳过真实 LLM、跳过真实 DB、跳过人工确认边界。

### Phase 10：部署与销售交付收口

- 目标：环境配置、部署说明、销售演示路径、客户交付边界。
- 完成标准：销售和交付团队可以按固定路径演示和交付；工程团队可以按文档部署、配置环境变量、初始化数据库并运行验收。
- 禁止扩展：P1/P2 能力、复杂商业化套餐、账号矩阵运营。

## 十二、Codex 执行规则

以后 Codex 每轮必须遵守：

1. 必须声明当前 Phase。
2. 每轮只做一个 Phase。
3. 未经允许不得跨 Phase。
4. 未经允许不得做 P1/P2。
5. 必须列出修改文件。
6. 必须说明调用哪些真实 API。
7. 必须运行对应验收脚本。
8. 必须运行 `check/test/build`。
9. 必须说明是否修改业务逻辑。
10. 遇到阻断必须停止，不得自行扩展绕过。

## 十三、每轮输出格式

每轮 Codex 完成后只回复：

1. 当前 Phase
2. 本轮目标
3. 修改文件
4. 实现了什么
5. 未实现什么
6. 调用的真实 API
7. 验收脚本结果
8. `check/test/build` 结果
9. 是否触碰禁止范围
10. 当前风险
11. 是否允许进入下一 Phase

## 十四、本 Harness 的禁止事项

本 Harness 建立后，后续工程不得用以下方式冒充 V1.0 完成：

1. 用 mock 数据冒充真实 DB 写入。
2. 用静态 demo 数据冒充客户项目。
3. 用 toast 或空按钮冒充真实保存。
4. 绕过 `protectedProcedure`。
5. 跳过真实 LLM 后直接写入诊断结果。
6. 直接写入 `geo_articles` 冒充文章生成。
7. 质量审查未通过仍显示可发布。
8. 未做反同质化却显示已通过。
9. 平台未授权却显示可自动发布。
10. 未经人工确认自动发布。
11. 自动收录监测或自动复测 AI 推荐。
12. 模拟登录、验证码处理、绕过平台风控。
13. 未经用户确认跨 Phase 修复。
14. 以 UI 美化替代主链路闭环。
