# P0.8 GEO 系统全链路回归验收报告：海豚知道

**作者：Manus AI**  
**验收对象：企业 AI 搜索增长工作台 / 海豚知道项目**  
**验收结论日期：2026-05-08**

## 一、主链路验收结果

本轮按“不要新增功能、不要扩展 P1/P2”的约束，围绕“海豚知道”项目完成了 P0.8 主链路回归验收。验收覆盖项目打开、轻量 Harness 状态机、问题库、AI 生成问题、客户指定问题入口、AI 回答导入、AI 语义分析、人工修订入口、GEO 评分、优化任务、内容模板、老板版 Markdown 报告、无数据阻断、中文文案、按钮与下一步提示。

总体结论是：**P0 主链路界面和核心数据表已连通，但当前“海豚知道”数据状态不足以建议进入真实客户试用**。本轮修复了一个明确会影响交付判断的 Harness 状态回退问题，并将 404 页剩余英文文案改为中文；修复后项目状态已从错误停留的 `score_done` 校正为 `report_ready`。但是，当前样本中客户指定问题数为 0、人工修订数为 0，报告长度仅 849 字，多数模板明显过短，且存在 `example.com` 示例发布链接，因此尚不能判定指定问题与人工修订链路在真实客户数据上完成端到端闭环。

| 验收维度 | 结果 | 证据摘要 |
|---|---:|---|
| P0 主链路完整性 | 部分通过 | 项目、问题、回答、分析、评分、任务、模板、报告数据均存在，但指定问题与人工修订真实样本缺失。 |
| 轻量 Harness | 修复后通过 | 项目状态已校正为 `report_ready`；新增测试覆盖状态只前进不回退。 |
| 指定问题进入链路 | 未通过 | 问题库有导入口和来源标识，但海豚知道当前 `specified_questions=0`。 |
| 人工修订影响评分、任务、模板、报告 | 不可判定 | 代码与页面提示存在，但海豚知道当前 `manual_review_count=0`，无端到端样本。 |
| 报告交付质量 | 未通过 | 报告存在但 `markdownContent` 长度为 849 字，未达到初步客户交付深度。 |
| 模板可复制质量 | 未通过 | 5 个模板均绑定任务，但 4 类模板长度仅 109–226 字。 |
| 英文占位符 | 修复后基本通过 | 发现并修复 404 页英文文案；未在已验收业务页面发现明显英文占位。 |
| 假数据伪装真实分析 | 存在风险 | 有示例域名发布链接 `https://example.com/haitunzhidao-hard-test`，不应作为真实完成状态。 |

## 二、各模块验收结论

| 模块 | 是否通过 | 说明 |
|---|---:|---|
| 创建/打开企业项目 | 通过 | 项目管理页可见“海豚知道”，企业信息、行业、官网、关键词和竞品信息可展示。 |
| 轻量 Harness 状态机 | 修复后通过 | 发现已有报告/模板但状态仍为 `score_done`；已修复状态不能回退，并校正历史数据为 `report_ready`。 |
| 问题库 | 部分通过 | 现有 60 条问题均可展示，入口与来源标识存在；但当前全部为 AI 生成问题，无客户指定问题样本。 |
| AI 自动生成问题 | 通过 | 当前存在 60 条 `ai_generated` 问题。 |
| 客户指定问题导入 | 未通过 | 页面有手动批量与 CSV 导入口，但当前项目无 `manual` 或 `csv` 来源问题，无法证明进入分析与报告链路。 |
| AI 回答导入 | 通过 | 当前存在 10 条真实导入 AI 回答，页面提示不允许空数据生成分析。 |
| AI 语义分析 | 通过 | 当前存在 10 条分析结果，页面可展示推荐、竞品、内容缺口和优化建议等结构化字段。 |
| 人工修订能力 | 部分通过 | 修订弹窗、字段、原始分析查看与撤销入口可见；但未实际保存修订样本，数据库人工修订数为 0。 |
| GEO 评分使用修订结果 | 不可判定 | 代码逻辑此前支持优先读取修订结果，但海豚知道无修订样本，本轮不能证明端到端结果变化。 |
| 优化任务使用修订结果 | 不可判定 | 当前有 7 条任务，但没有修订样本驱动的重新生成证据。 |
| 内容模板绑定任务 | 通过 | 5 个内容模板均有 `optimization_task_id` 绑定。 |
| 内容模板质量 | 未通过 | FAQ、竞品对比、客户案例、行业选型文章模板长度明显不足，不适合作为客户可复制初稿。 |
| 老板版 Markdown 报告 | 未通过 | 报告存在并包含总分与摘要，但长度 849 字，缺少足够诊断深度。 |
| 无数据阻断假报告 | 部分通过 | 页面与逻辑有阻断提示；但示例发布链接会造成“真实完成”误判风险。 |
| 状态、按钮、下一步提示 | 修复后部分通过 | 主状态修复为 `report_ready`；仍建议在真实重跑指定问题/人工修订后复测所有下一步提示。 |

## 三、发现的问题清单

| 编号 | 严重度 | 问题 | 当前处理 |
|---|---:|---|---|
| P0.8-01 | 高 | 有报告和模板时项目状态仍停留在 `score_done`，导致 Harness 下一步提示错误。 | 已修复状态推进逻辑，并校正历史数据。 |
| P0.8-02 | 高 | 海豚知道当前客户指定问题数为 0，无法证明指定问题进入回答、分析、评分和报告链路。 | 未改数据；记录为验收未通过。 |
| P0.8-03 | 高 | 海豚知道当前人工修订数为 0，无法证明修订结果影响评分、任务、模板和报告。 | 未造测试数据；记录为不可判定。 |
| P0.8-04 | 高 | 老板版 Markdown 报告长度仅 849 字，达不到真实客户初步交付质量。 | 未扩展功能；记录为必须优先修复/重跑。 |
| P0.8-05 | 高 | FAQ、竞品对比、客户案例、行业选型文章模板过短，不具备可复制初稿质量。 | 未扩展内容生成；记录为必须优先修复/重跑。 |
| P0.8-06 | 中 | 一条已完成任务的发布链接为 `example.com` 示例链接，存在伪装真实交付风险。 | 未删除业务数据；记录为真实交付前必须清理。 |
| P0.8-07 | 中 | 404 页面存在英文文案。 | 已修复为中文。 |

## 四、必须优先修复的问题

在进入真实客户试用前，必须优先处理三类问题。第一，使用“海豚知道”真实新增至少 1 条手动指定问题和 1 条 CSV 指定问题，并让它们进入 AI 回答、语义分析、评分和报告统计链路。第二，实际保存至少 1 条人工修订，并重跑评分、任务、模板和报告，验证所有下游产物优先使用修订结果。第三，重新生成或修复当前过短的老板版报告和 4 类内容模板，确保报告与模板达到可被客户阅读、复制和执行的初稿质量。

| 优先级 | 修复项 | 判定标准 |
|---|---|---|
| P0 | 指定问题真实样本进入全链路 | 报告中能看到指定问题统计，且对应回答、分析和评分样本存在。 |
| P0 | 人工修订真实样本影响下游 | 修订后评分、任务、模板、报告内容均体现修订字段。 |
| P0 | 报告质量达标 | 老板版报告具备完整章节、真实指标解释、问题诊断和 30 天行动建议。 |
| P0 | 模板质量达标 | 每类模板可直接复制给内容团队使用，而不是短摘要。 |
| P0 | 清理示例链接 | 不允许 `example.com` 作为已完成任务的真实发布地址。 |

## 五、暂时可以接受的风险

当前可以暂时接受的是：AI 生成问题、AI 回答导入、语义分析、GEO 评分、任务与模板表之间的基础数据结构已经连通；页面主体文案为中文；无数据阻断提示在关键页面存在；轻量 Harness 状态回退问题已修复并有测试覆盖。这些风险可以在内部灰度继续观察，但不应作为“真实客户已可试用”的依据。

| 风险 | 是否可暂时接受 | 理由 |
|---|---:|---|
| 仅有 AI 生成问题，没有指定问题样本 | 否 | 这是本轮重点验收项，必须补真实样本。 |
| 无人工修订样本 | 否 | 无法证明修订影响评分、任务、模板、报告。 |
| 报告和模板过短 | 否 | 直接影响客户交付观感。 |
| 状态机历史数据曾错乱 | 是 | 已修复并补充测试，但仍需真实重跑复测。 |
| 少量非业务页面英文文案 | 是 | 404 英文已修复，业务页面未发现明显英文占位。 |

## 六、是否建议进入真实客户试用

**不建议现在进入真实客户试用。** 当前系统适合继续作为内部 P0 回归环境或客户演示前的候选版本，但不适合作为真实客户初步交付版本。原因并不是主链路完全不可用，而是两个关键验收目标没有真实样本支撑：客户指定问题没有进入实际分析与报告链路，人工修订没有实际影响下游产物；同时报告和模板质量不足，会在客户侧形成“结果太浅、像半成品”的直接风险。

## 七、改动文件

| 文件 | 改动说明 |
|---|---|
| `server/routers.ts` | 修复 Harness 状态更新逻辑，状态只能向前推进，避免从 `report_ready` 回退到 `score_done`。 |
| `server/statusMachine.test.ts` | 新增状态机单元测试，覆盖向前推进、禁止回退和空状态初始化。 |
| `client/src/pages/NotFound.tsx` | 将 404 页面英文文案改为中文。 |
| `todo.md` | 记录本轮 P0.8 回归验收范围、结论和未通过项。 |
| `p0_8_regression_notes.md` | 保存本轮逐页验收与数据库复核原始记录。 |
| `P0_8_海豚知道_GEO全链路回归验收报告.md` | 本报告。 |

另有一次数据库修复：已将存在报告或内容模板的项目状态校正为 `report_ready`，并确认“海豚知道”状态已为 `report_ready`。

## 八、如何复测

复测时应先打开“海豚知道”项目，确认顶部 Harness 状态为 `report_ready`，并逐页检查项目管理、问题库、AI 回答导入、AI 语义分析、GEO 评分、优化工作台、内容模板与报告。随后应新增真实指定问题样本，导入对应 AI 回答，执行语义分析，保存一条人工修订，再重新生成评分、任务、模板和报告，检查报告中是否出现指定问题统计和人工修订后的结论。

| 复测步骤 | 期望结果 |
|---|---|
| 运行 `pnpm check && pnpm test` | TypeScript 通过，Vitest 3 个文件 13 个用例通过。 |
| 查看“海豚知道”项目状态 | 状态为 `report_ready`，不会因重新评分回退。 |
| 新增手动指定问题与 CSV 指定问题 | 问题来源分别显示为手动/CSV，统计不再为 0。 |
| 导入指定问题对应 AI 回答并分析 | 回答与分析数量增加，且能追溯到指定问题。 |
| 保存人工修订并重跑评分 | 评分指标使用修订后的提及、推荐、胜出、错误认知字段。 |
| 重跑任务、模板和报告 | 任务原因、模板内容和报告诊断体现修订后的内容缺口与建议。 |
| 检查报告和模板 | 报告达到可读诊断深度，模板可复制执行，无示例链接或英文占位。 |

## 九、真实风险

真实风险主要集中在客户信任层面。如果当前版本直接给真实客户试用，客户可能会看到较短的报告、过短的模板、没有指定问题结论、没有人工修订影响痕迹，以及示例域名发布链接。这些问题会让客户误以为系统在用半成品或假数据生成结论。建议先完成上述 P0 修复与真实样本复测，再考虑进入小范围客户试用。

## 十、测试结果

本轮已运行 `pnpm check && pnpm test`。TypeScript 类型检查通过，Vitest 共 3 个测试文件、13 个测试用例全部通过。最终开发服务状态为 running，LSP 无错误，TypeScript 无错误，依赖检查通过。
