# GEO 工作台 · 系统性可用性分析报告

**报告日期**：2026-06-22  
**测试范围**：前端所有主链路页面（12页）+ 关键 Bug 代码层根因分析  
**测试账号**：419052760@qq.com（文召王）  
**测试项目**：海豚知道（ID 180001，创建于 2026-06-17）  
**部署地址**：https://aigeoworkb-kzxhj9uy.manus.space

---

## 一、测试覆盖总览

### 主链路页面（10步）

| # | 页面 | 路由 | 页面状态 | 数据状态 | 备注 |
|---|------|------|----------|----------|------|
| 1 | 客户管理台 | `/clients` | ✅ 正常 | ✅ 有数据 | 显示「海豚知道」项目卡片 |
| 2 | 工作台首页 | `/workspace` | ✅ 正常 | ⚠️ 数据偏低 | 成熟度 29分，显示「建档初始阶段」 |
| 3 | 企业建档 | `/enterprise-profile` | ✅ 正常 | ⚠️ 未完成 | 8步向导完成 4/8（47%） |
| 4 | AI 实测诊断 | `/ai-diagnosis` | ✅ 正常 | ⏳ 待首次诊断 | 需手动发起实测 |
| 5 | 成熟度详情 | `/maturity` | ✅ 正常 | ✅ 有数据 | 6维评分展示正常 |
| 6 | 月度优化计划 | `/monthly-plan` | ✅ 正常 | ⏳ 待生成 | 需先达到品牌档案就绪 |
| 7 | 内容生产 | `/weekly` | ✅ 正常 | ⏳ 依赖诊断 | 8平台账号未绑定警告 |
| 8 | 内容发布 | `/content-publishing` | ✅ 正常 | 🔴 客户端未连 | 本地客户端未连接 |
| 9 | 收录监测 | `/inclusion-monitoring` | ✅ 正常 | ⏳ 无发布内容 | 有孤儿复测数据（P2） |
| 10 | 交付报告 | `/delivery-reports` | ✅ 正常 | ⏳ 待首月生成 | 需月度计划先执行 |

### 资产管理页面

| 页面 | 路由 | 状态 | 备注 |
|------|------|------|------|
| 问题库 | `/questions` | ✅ 正常，⚠️ 有UI Bug | 显示 33 题总数，但分组全为 0 |
| 信源图谱 | `/brand-source-graph` | ✅ 正常 | AI 自动发现信源功能可用 |

**结论：12 个页面全部可访问，无崩溃、无白屏。主链路流程逻辑完整，空态引导清晰。**

---

## 二、Bug 清单（按优先级）

---

### 🔴 P0 · 直接 URL 导航失效

**现象**：用浏览器刷新页面（如 F5）、或从外部打开带 `?projectId=N` 的链接，出现「项目不存在」toast 并跳转回 `/clients`，即使 ID 完全正确。

**复现步骤**：
1. 在工作台任意页面按 F5 刷新
2. 出现「项目不存在」toast，跳转 `/clients`
3. 需要重新点进项目才能恢复

**影响范围**：
- 书签无法使用
- 无法向同事分享页面链接
- 页面刷新必然丢失上下文
- 所有带 `?projectId=N` 的 URL 作为入口均不可靠

**根因分析（代码层）**：

问题根源在 `useActiveProjectSelection.ts`（第 23 行）：

```typescript
// 无 enabled 守卫，直接发起请求
const { data: projectsRaw = [], isLoading: projectsLoading } = 
  trpc.geo.projects.list.useQuery();
```

`geo.projects.list` 使用 `protectedProcedure`。当全页面刷新时，若 tRPC 请求因 session 问题（cookie 失效、服务器冷启动、网络抖动）返回错误，React Query 将：
- `isLoading = false`（请求结束，只是失败了）
- `data = undefined` → 默认值 `[]`（空数组）

此时 `useInvalidProjectRedirect` 看到的是：
```
projectsLoading = false
projects = []
contextProjectId = 180001（来自 URL）
isProjectIdAccessible(180001, []) = false → 触发重定向！
```

hook 错误地将「数据加载失败」等同于「项目不存在」。

**修复方案**（Cursor 指令可直接用）：

在 `useActiveProjectSelection.ts` 中加入 `isError` 守卫：

```typescript
const { 
  data: projectsRaw = [], 
  isLoading: projectsLoading,
  isError: projectsError   // ← 新增
} = trpc.geo.projects.list.useQuery();

// inspection 计算中加入 isError 判断
const inspection = useMemo(() => {
  if (projectsLoading || projectsError) {  // ← 加入 projectsError
    return { projectId: null, contextId: null, staleContext: false };
  }
  return inspectActiveProjectContext(projects, { search });
}, [projectsLoading, projectsError, projects, search]);

// 传给 useInvalidProjectRedirect 时同样屏蔽
useInvalidProjectRedirect({
  projectsLoading: projectsLoading || projectsError,  // ← 合并
  projects,
  contextProjectId: inspection.contextId,
});
```

同时将 `activeProjectId` 同步写入 `localStorage`（当前只写 `sessionStorage`），使全页面刷新后 projectId 不丢失：

```typescript
// activeProject.ts：setActiveProjectId 时同时写 localStorage
export function setActiveProjectId(projectId: number | string): void {
  const id = sanitizeActiveProjectId(parseProjectId(projectId));
  if (!id) return;
  if (typeof window !== "undefined") {
    sessionStorage.setItem(STORAGE_KEY, String(id));
    localStorage.setItem(STORAGE_KEY, String(id));   // ← 新增
  }
}

// getActiveProjectIdFromStorage 时 sessionStorage 优先，fallback localStorage
export function getActiveProjectIdFromStorage(): number | null {
  const fromSession = sessionStorage.getItem(STORAGE_KEY);
  if (fromSession) return sanitizeActiveProjectId(Number.parseInt(fromSession, 10));
  const fromLocal = localStorage.getItem(STORAGE_KEY);  // ← 新增 fallback
  if (fromLocal) return sanitizeActiveProjectId(Number.parseInt(fromLocal, 10));
  return null;
}
```

**优先级**：上线前必修，否则用户无法正常使用书签和分享链接。

---

### 🟡 P1-A · 问题库分组全部显示 (0)

**现象**：问题库顶部指标显示「问题总数：33」，但「按类型分组」区域的所有标签页均显示 (0)，点进去无问题列表。

**根因分析**：

`groupQuestionsBySearchPoolType`（`shared/questionSearchPool.ts` 第 203 行）只分配有 `searchPoolType` 值的问题：

```typescript
for (const question of questions) {
  const key = question.searchPoolType;
  if (key && key in grouped) {  // ← null/undefined 的问题被静默丢弃
    grouped[key as SearchPoolQuestionType].push(question);
  }
}
```

现有 33 道问题是手工创建的，`searchPoolType` 字段为 `null`（字段是后来新增的）。`buildQuestionPoolGapOverview` 的 `totalQuestions` 直接取 `questions.length`（含 null 类型），而分组计数用的是过滤后的结果，导致数字不一致。

**影响**：用户看到「有33道题」，但完全无法按类型管理，困惑且无法操作。

**修复方案**：

方案 A（推荐）—— 批量回填 `searchPoolType`：在 `generateSearchPool` mutation 中，对当前项目所有 `searchPoolType = null` 的题目，依据 `questionType` 做映射回填：

```typescript
// 在 generateSearchPool 之前或之后加一次 backfill
await db.update(questions)
  .set({ searchPoolType: mapLegacyTypeToSearchPoolType(questionType) })
  .where(and(
    eq(questions.projectId, input.projectId),
    isNull(questions.searchPoolType)
  ));
```

方案 B —— 增加「未分类」兜底分组，让 null 类型问题可见。

---

### 🟡 P1-B · 成熟度数据差异（系统说明文档 vs 实际）

**现象**：用户系统说明文档记载的成熟度为 58分 / 品牌提及率 41% / AI推荐率 28%，但实际生产系统显示 29分 / -- / --。

**根因分析（非 Bug）**：

这不是系统 Bug，而是数据状态差异：

| 项 | 说明文档值 | 实际系统值 | 原因 |
|---|---|---|---|
| 成熟度总分 | 58分 | 29分 | 说明文档基于充分填写状态的演示数据；当前项目企业建档仅完成47% |
| 品牌提及率 | 41% | --（无数据） | AI诊断未执行过（项目创建5天，暂无实测记录） |
| AI推荐率 | 28% | --（无数据） | 同上 |

6 维评分当前状态（系统已计算一次，2026-06-17）：
- 品类定位清晰度：偏低（竞品、目标客户未填写）
- 公开信源完整度：0（信源图谱空）
- 信任证据强度：0（无案例、证书、评价）
- AI实测表现：0（无诊断数据）

**行动建议**：完成企业建档剩余4步 → 发起一次 AI 实测诊断 → 重新计算成熟度，分数预计可升至 45-55分。

---

### 🟡 P1-C · 本地发布客户端未连接

**现象**：内容发布、平台账号等页面均显示「本地客户端未连接」，发布任务无法执行。

**根因**：本地发布客户端（Electron）需在操作者本地电脑安装并运行，生产环境无此条件。

**影响**：整个内容发布工作流（步骤7-9）无法执行，包括：
- 内容自动发布到知乎/头条/百家号等平台
- 平台账号 session 同步
- 发布任务调度

**解决路径**：
1. 在运营人员本机安装 Electron 本地客户端
2. 或评估云端自动化方案（当前架构支持 agentRouter 云端调度，但需平台账号托管方案配合）

---

### 🟢 P2 · 收录监测孤儿数据

**现象**：收录监测页显示「待7天后复测 1、待14天后复测 1、待30天后复测 1」，但已发布内容数为 0。

**根因**：数据库中存在早期测试写入的 `geoInclusionMonitoringRecords` 记录，但对应的 `geoPublishRecords` 不存在或已删除，产生孤儿数据。

**影响**：UI 数字不一致，影响信任度，但不影响核心功能。

**修复**：清除孤儿监测记录，或在查询时做 JOIN 过滤（`INNER JOIN geoPublishRecords`）。

---

## 三、10步主链路 · 逐步完成度评估

```
步骤1  建立企业档案    ████████░░  80%  已完成 4/8 模块，缺竞品/信任证据/信源/90天目标
步骤2  生成问题库      ██████████  95%  33道题已就绪，但分组 UI 有 Bug
步骤3  AI 实测诊断     ░░░░░░░░░░   0%  未执行
步骤4  成熟度评分      ████░░░░░░  40%  已有29分基础分，等数据补全后重算
步骤5  月度计划        ░░░░░░░░░░   0%  依赖成熟度就绪
步骤6  内容生产        ░░░░░░░░░░   0%  依赖月度计划 + 本地客户端
步骤7  内容发布        ░░░░░░░░░░   0%  阻塞：客户端未连接 + 8平台未绑定
步骤8  收录监测        ░░░░░░░░░░   0%  依赖发布
步骤9  交付报告        ░░░░░░░░░░   0%  依赖月度数据
步骤10 优化迭代        ░░░░░░░░░░   0%  依赖完整数据
```

**现状评估**：系统处于「建档初始阶段」，前2步基础工作已完成，步骤3（AI 实测诊断）是当前首要解锁节点。

---

## 四、UI/UX 亮点（运营实际反馈）

以下功能在测试中表现良好，是系统的竞争力亮点：

1. **全局「继续完成品牌建档」悬浮按钮**：顶部右侧引导入口，随时提醒进度，体验好。
2. **右侧「行动指引」面板**：每个页面的右侧栏都有结构化的下一步建议，配合整体流程感强。
3. **工作台「当前阶段」自动判断**：根据完成度自动显示「建档初始阶段」并给出 CTA，省去用户判断。
4. **6维成熟度雷达图 + 优先级排序**：成熟度详情页布局清晰，按权重排序的「重点提升」提示实用。
5. **客户管理台卡片布局**：多客户管理视角清晰，项目状态一目了然。
6. **空态引导设计**：所有尚未激活的功能页面都有清晰的空态说明和操作引导，不会让用户困惑。

---

## 五、优先修复建议

### 立即修复（影响基本可用性）

| 优先级 | 问题 | 修复复杂度 | 预估工时 |
|--------|------|-----------|----------|
| P0 | URL 直接导航 / 刷新失效 | 中（2处代码改动） | ~2h |
| P1-A | 问题库分组全显 0 | 低（DB 回填脚本） | ~1h |

### 后续迭代（V2.1）

| 优先级 | 问题 | 建议方案 |
|--------|------|---------|
| P1-C | 本地客户端连接引导 | 优化下载/连接引导流程；评估云端发布方案 |
| P2 | 收录监测孤儿数据 | JOIN 查询过滤，或一次性清表脚本 |
| 数据完善 | 企业建档剩余4步 | 运营填写，非工程问题 |

---

## 六、总体可用性评定

| 维度 | 评分 | 说明 |
|------|------|------|
| **页面完整性** | ⭐⭐⭐⭐⭐ | 12个页面全部可访问，无崩溃 |
| **流程逻辑** | ⭐⭐⭐⭐⭐ | 10步主链路逻辑完整，依赖关系正确 |
| **导航稳定性** | ⭐⭐⭐☆☆ | SPA内导航正常；刷新/外链失效（P0 Bug） |
| **数据可靠性** | ⭐⭐⭐⭐☆ | 显示数据准确；部分孤儿数据影响小 |
| **UI/UX 体验** | ⭐⭐⭐⭐☆ | 空态设计好，问题库分组显示有 Bug |
| **运营就绪度** | ⭐⭐⭐☆☆ | 建档完成度47%，发布链路待接通 |

**综合评定：系统架构完整，主链路页面全部可用，有 1 个 P0 Bug 需要在正式交付客户前修复。修复 P0 + 完成企业建档后，系统可进入首次 AI 诊断阶段。**

---

*报告生成时间：2026-06-22 | 测试方法：浏览器端页面访问 + 代码根因分析*
