# `/flow` 浏览器验收记录

访问地址：`/flow`。

首屏可见标题为“AI GEO 试跑向导”，顶部展示“当前项目”“当前步骤 6/6”“本轮试跑闭环已完成”的当前进度、当前建议卡片，以及“继续当前步骤”按钮。左侧仍有 6 个主流程菜单：总览、企业档案、AI 诊断、内容生成、内容发布、收录监测、交付报告，未出现支付、多租户、权限、自动抓取或第三方自动发布入口。

向下滚动后可见 6 张步骤卡：企业档案、AI 诊断、内容生成、内容发布、收录监测、交付报告。每张卡有“已完成”状态标识、完成标准区、高级能力状态区和一个“查看本步骤”主按钮。当前样例状态显示 6/6 已完成，因此后续步骤未触发“需先完成上一环节”的未完成态视觉；该前置提示需要通过测试或源码逻辑继续核验。

内容发布步骤卡可见“第 4 步 内容发布”，描述为确认可发布内容、完成系统内置 GEO 内容页发布与第三方素材准备；完成标准为“至少一篇内容进入已发布状态，或已有对应发布记录可核验”；高级能力状态仅展示“发布前检查”“第三方素材”。

## 内容发布步骤浏览器验收记录

访问地址：`/content-publishing`，由 `/flow` 第 4 步对应路径指向。

页面顶部显示当前阶段为“内容发布”，当前完成度 86%，下一步动作是“进入收录监测”，并有风险提醒：“第三方平台素材仅用于复制参考，当前不自动登录外部平台发布。”页面主体显示“可发布内容”和“已发布内容”两个区块，能够看到多条可发布文章标题，状态文案为“允许发布：已满足当前发布准入。”；已发布区块能够看到内容编号与“状态：已发布”。首屏交互元素包括“进入收录监测”和折叠的“第三方平台素材”。

风险观察：浏览器提取文本与当前视口中未直接看到“质量分”“发布前检查状态”“推荐平台：GEO 内容页”“发布到 GEO 内容页”这几个字面字段；由于本轮要求只验收、不开发，这些点需要结合源码或测试判断是否构成阻断。若正式友好试跑强调销售现场一步步演示内容发布页，上述字段的可见性不足属于可接受风险或轻度缺口，但目前页面仍能看到可发布内容、已发布内容、已发布状态、进入收录监测和第三方素材辅助说明。


## 公开内容页验收记录

访问地址：`/geo/content/1/180001`。

首屏呈现为正式文章卡片，包含文章标题、文章类型、发布状态、质量评分、发布时间/更新时间和风险提示。页面未出现 `Formal Article`、`这是一篇面向外部访客阅读的正式 GEO 文章` 或 `当前资产库证据缺口为：暂无关键证据缺口`。风险提示明确写明“不承诺保证排名、保证收录或保证被 AI 推荐”。

发现并仅修复一处阻断验收问题：AI 可引用摘要中的短答案因截断拼接出现双句号和未闭合引号片段。复验后，相关短答案显示为“海豚知道需要补齐的是围绕内容缺口。”，不再出现双句号或未闭合引号。公开内容页仍保持“查看生成依据与事实溯源”为折叠辅助区，首屏不默认展开后台审计信息。
