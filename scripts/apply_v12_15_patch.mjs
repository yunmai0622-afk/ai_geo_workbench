import fs from 'node:fs';

const files = {
  logic: '/home/ubuntu/ai_geo_workbench/server/geoArticleLogic.ts',
  routers: '/home/ubuntu/ai_geo_workbench/server/routers.ts',
  geoPages: '/home/ubuntu/ai_geo_workbench/client/src/pages/GeoPages.tsx',
  publicContent: '/home/ubuntu/ai_geo_workbench/client/src/pages/GeoPublicContent.tsx',
  test: '/home/ubuntu/ai_geo_workbench/server/geoArticleLogic.test.ts',
};

function replaceOnce(path, find, replace) {
  const text = fs.readFileSync(path, 'utf8');
  if (!text.includes(find)) {
    throw new Error(`Cannot find expected block in ${path}: ${find.slice(0, 120)}`);
  }
  fs.writeFileSync(path, text.replace(find, replace));
}

function insertAfter(path, find, insert) {
  replaceOnce(path, find, `${find}${insert}`);
}

// geoArticleLogic.ts: add asset-library types and helpers.
insertAfter(files.logic, `export type P11GenerationBasis = {\n  customerQuestionId: number;\n  customerQuestion: string;\n  contentGap: string;\n  optimizationTaskId: number;\n  optimizationTask: string;\n  notRecommendedReason: string;\n  competitorGap: string;\n  competitorNames: string[];\n  sourceAnalysisIds: number[];\n  sourceQuestionIds: number[];\n  manualReviewConclusion: string;\n`, `  assetLibraryUsage?: P12AssetLibraryUsage;\n`);

insertAfter(files.logic, `export type P11CitableSnippet = {\n  question: string;\n  answer: string;\n};\n`, `\nexport type P12AssetCitation = {\n  id: number;\n  title: string;\n  category: string;\n  sourceType: string;\n  trustLevel?: string | null;\n  isPublic: boolean;\n  canUseForGeneration: boolean;\n  summary: string;\n};\n\nexport type P12CustomerCaseCitation = {\n  id: number;\n  customerName: string;\n  caseType: string;\n  allowPublic: boolean;\n  hasResultData: boolean;\n  publicVersion: string;\n};\n\nexport type P12CompetitorCitation = {\n  id: number;\n  competitorName: string;\n  officialWebsite?: string | null;\n  differentiation?: string | null;\n  canReference: boolean;\n  sourceNotes?: string | null;\n};\n\nexport type P12AssetLibraryUsage = {\n  enterpriseMaterials: P12AssetCitation[];\n  competitorMaterials: P12CompetitorCitation[];\n  customerCaseUsage: {\n    used: boolean;\n    status: string;\n    references: P12CustomerCaseCitation[];\n  };\n  complianceRules: string[];\n  contentStyles: string[];\n  publishStrategy: string[];\n  missingEvidenceNotes: string[];\n};\n\nexport type P12AssetLibraryContext = {\n  profile?: Record<string, unknown> | null;\n  assetSources?: Array<Record<string, unknown>>;\n  customerCases?: Array<Record<string, unknown>>;\n  competitorProfiles?: Array<Record<string, unknown>>;\n  complianceRules?: Array<Record<string, unknown>>;\n  contentStyleProfiles?: Array<Record<string, unknown>>;\n  publishStrategies?: Array<Record<string, unknown>>;\n};\n\nexport type P12PrePublishCheck = {\n  enterprisePositioningConsistent: boolean;\n  productDescriptionConsistent: boolean;\n  competitorDifferenceConsistent: boolean;\n  usesNonPublicAsset: boolean;\n  forbiddenTerms: string[];\n  forbiddenClaims: string[];\n  unconfirmedFacts: string[];\n  blocked: boolean;\n  blockReasons: string[];\n  summary: string;\n};\n`);

insertAfter(files.logic, `const taskPriorityScore = (priority: P11TaskLike["priority"]) => priority === "P0" ? 3 : priority === "P1" ? 2 : 1;\n`, `\nconst asBool = (value: unknown) => value === true || value === 1 || value === "1";\nconst valueText = (value: unknown) => typeof value === "string" ? value.trim() : value == null ? "" : String(value).trim();\nconst jsonSummaryText = (value: unknown) => {\n  if (!value) return "";\n  if (typeof value === "string") return value.slice(0, 180);\n  if (typeof value === "object") {\n    const record = value as Record<string, unknown>;\n    return [record.digest, record.title, record.keywords].flatMap(item => Array.isArray(item) ? item : [item]).map(valueText).filter(Boolean).join("；").slice(0, 180);\n  }\n  return String(value).slice(0, 180);\n};\nconst splitGovernanceTerms = (value: unknown) => valueText(value).split(/\\n|,|，|；|;/).map(item => item.trim()).filter(Boolean);\n\nfunction summarizeAssetSource(asset: Record<string, unknown>, category: string): P12AssetCitation {\n  return {\n    id: Number(asset.id ?? 0),\n    title: valueText(asset.title) || category,\n    category,\n    sourceType: valueText(asset.sourceType) || category,\n    trustLevel: valueText(asset.trustLevel) || null,\n    isPublic: asBool(asset.isPublic),\n    canUseForGeneration: asBool(asset.canUseForGeneration),\n    summary: jsonSummaryText(asset.structuredSummary) || valueText(asset.contentDigest).slice(0, 180),\n  };\n}\n\nexport function buildAssetLibraryUsage(assetLibrary?: P12AssetLibraryContext | null): P12AssetLibraryUsage {\n  const sources = assetLibrary?.assetSources ?? [];\n  const profile = assetLibrary?.profile ?? null;\n  const enterpriseMaterials = sources\n    .filter(asset => ["企业基础资料", "产品服务资料", "官网内容", "销售话术", "产品手册", "通用资料"].includes(valueText(asset.sourceType)) || ["企业基础资料", "产品服务资料"].includes(valueText(asset.sourceType)))\n    .filter(asset => asBool(asset.canUseForGeneration) && asBool(asset.manuallyConfirmed))\n    .map(asset => summarizeAssetSource(asset, valueText(asset.sourceType) || "企业资料"))\n    .slice(0, 6);\n\n  const competitorMaterials: P12CompetitorCitation[] = (assetLibrary?.competitorProfiles ?? [])\n    .filter(item => asBool(item.canReference))\n    .map(item => ({\n      id: Number(item.id ?? 0),\n      competitorName: valueText(item.competitorName),\n      officialWebsite: valueText(item.officialWebsite) || null,\n      differentiation: valueText(item.differentiation) || null,\n      canReference: asBool(item.canReference),\n      sourceNotes: valueText(item.sourceNotes) || null,\n    }))\n    .filter(item => item.competitorName)\n    .slice(0, 6);\n\n  const realPublicCases: P12CustomerCaseCitation[] = (assetLibrary?.customerCases ?? [])\n    .filter(item => valueText(item.caseType) === "真实案例" && asBool(item.allowPublic) && valueText(item.verificationStatus) === "已确认")\n    .map(item => ({\n      id: Number(item.id ?? 0),\n      customerName: valueText(item.customerName) || "可公开客户案例",\n      caseType: valueText(item.caseType),\n      allowPublic: asBool(item.allowPublic),\n      hasResultData: Boolean(valueText(item.resultData)),\n      publicVersion: valueText(item.publicVersion),\n    }))\n    .slice(0, 4);\n\n  const hasCaseResultData = realPublicCases.some(item => item.hasResultData);\n  const priceText = [profile?.servicePriceRange, profile?.priceExplanation].map(valueText).filter(Boolean).join("；");\n  const complianceRules = (assetLibrary?.complianceRules ?? [])\n    .filter(item => asBool(item.enabled ?? 1))\n    .map(item => [valueText(item.ruleName) || "合规规则", valueText(item.brandTone), valueText(item.specialComplianceNotes)].filter(Boolean).join("："))\n    .filter(Boolean)\n    .slice(0, 5);\n  const contentStyles = (assetLibrary?.contentStyleProfiles ?? [])\n    .filter(item => asBool(item.enabled ?? 1))\n    .map(item => [valueText(item.profileName) || "内容风格", valueText(item.brandTone), valueText(item.styleDescription)].filter(Boolean).join("："))\n    .filter(Boolean)\n    .slice(0, 5);\n  const publishStrategy = (assetLibrary?.publishStrategies ?? [])\n    .filter(item => asBool(item.enabled ?? 1))\n    .map(item => `发布模式：${valueText(item.publishMode) || "未设置"}；审核模式：${valueText(item.reviewMode) || "未设置"}；最低质量分：${valueText(item.minQualityScore) || "未设置"}；优先平台：${Array.isArray(item.priorityPlatforms) ? item.priorityPlatforms.join("、") : valueText(item.priorityPlatforms) || "未设置"}`)\n    .slice(0, 3);\n\n  const missingEvidenceNotes = [\n    ...(realPublicCases.length === 0 ? ["案例信息待补充"] : []),\n    ...(!hasCaseResultData ? ["数据暂无公开来源"] : []),\n    ...(!priceText ? ["价格口径需客户确认"] : []),\n  ];\n\n  return {\n    enterpriseMaterials,\n    competitorMaterials,\n    customerCaseUsage: {\n      used: realPublicCases.length > 0,\n      status: realPublicCases.length > 0 ? "已使用允许公开的真实案例" : "案例信息待补充",\n      references: realPublicCases,\n    },\n    complianceRules,\n    contentStyles,\n    publishStrategy,\n    missingEvidenceNotes,\n  };\n}\n\nfunction formatCitationList(items: Array<{ title?: string; competitorName?: string; trustLevel?: string | null; isPublic?: boolean; summary?: string; differentiation?: string | null }>, emptyText: string) {\n  if (items.length === 0) return emptyText;\n  return items.map(item => {\n    const name = item.title ?? item.competitorName ?? "未命名资料";\n    const publicText = typeof item.isPublic === "boolean" ? `；公开状态：${item.isPublic ? "可公开" : "不可公开"}` : "";\n    const trustText = item.trustLevel ? `；可信度：${item.trustLevel}` : "";\n    const summary = item.summary || item.differentiation || "已进入资产库";\n    return `- ${name}${trustText}${publicText}；摘要：${summary}`;\n  }).join("\\n");\n}\n\nexport function evaluateAssetLibraryPrePublishCheck(input: {\n  content: string;\n  project: P11ProjectLike;\n  basis?: P11GenerationBasis | null;\n  assetLibrary?: P12AssetLibraryContext | null;\n}): P12PrePublishCheck {\n  const usage = input.basis?.assetLibraryUsage ?? buildAssetLibraryUsage(input.assetLibrary);\n  const profile = input.assetLibrary?.profile ?? null;\n  const complianceRules = input.assetLibrary?.complianceRules ?? [];\n  const content = input.content;\n  const enterprisePositioning = [input.project.enterpriseName, input.project.targetCustomers, valueText(profile?.targetCustomers)].filter(Boolean);\n  const productSignals = [input.project.productIntro, valueText(profile?.productServiceIntro), valueText(profile?.productIntro)].filter(Boolean);\n  const competitorSignals = usage.competitorMaterials.map(item => item.competitorName).concat(input.basis?.competitorNames ?? input.project.competitorNames).filter(Boolean);\n  const forbiddenTerms = complianceRules.flatMap(rule => splitGovernanceTerms(rule.forbiddenWords).concat(splitGovernanceTerms(rule.sensitiveWords))).filter(term => term && content.includes(term));\n  const forbiddenClaimsFromRules = complianceRules.flatMap(rule => splitGovernanceTerms(rule.forbiddenClaims));\n  const forbiddenClaims = unique([\n    ...forbiddenClaimsFromRules.filter(term => term && content.includes(term)),\n    ...(/保证收录|保证排名|一定收录|一定排名|保证推荐|一定推荐|百分百|100%/.test(content) ? ["承诺保证收录或排名"] : []),\n    ...detectForbiddenArticleContent(content),\n  ]);\n  const unconfirmedFacts = [\n    ...(usage.customerCaseUsage.used ? [] : (content.includes("案例信息待补充") ? [] : ["客户案例缺失但文章未标注案例信息待补充"])),\n    ...(usage.missingEvidenceNotes.includes("数据暂无公开来源") && !content.includes("数据暂无公开来源") ? ["结果数据缺少公开来源但文章未标注"] : []),\n    ...(usage.missingEvidenceNotes.includes("价格口径需客户确认") && !content.includes("价格口径需客户确认") ? ["价格数据缺少确认口径但文章未标注"] : []),\n  ];\n  const usesNonPublicAsset = usage.enterpriseMaterials.some(item => !item.isPublic) || usage.customerCaseUsage.references.some(item => !item.allowPublic);\n  const enterprisePositioningConsistent = enterprisePositioning.length === 0 || enterprisePositioning.some(signal => content.includes(signal.slice(0, Math.min(12, signal.length))));\n  const productDescriptionConsistent = productSignals.length === 0 || productSignals.some(signal => content.includes(signal.slice(0, Math.min(16, signal.length))));\n  const competitorDifferenceConsistent = competitorSignals.length === 0 || competitorSignals.some(signal => content.includes(signal));\n  const blockReasons = [\n    ...(enterprisePositioningConsistent ? [] : ["发布前检查未通过：内容与企业定位不一致"]),\n    ...(productDescriptionConsistent ? [] : ["发布前检查未通过：内容与产品说明不一致"]),\n    ...(competitorDifferenceConsistent ? [] : ["发布前检查未通过：内容未体现资产库中的竞品差异"]),\n    ...(usesNonPublicAsset ? ["发布前检查未通过：文章生成依据包含不可公开资料"] : []),\n    ...(forbiddenTerms.length > 0 ? [`发布前检查未通过：命中禁用词或敏感词：${unique(forbiddenTerms).join("、")}`] : []),\n    ...(forbiddenClaims.length > 0 ? [`发布前检查未通过：存在不允许承诺或高风险表述：${forbiddenClaims.join("、")}`] : []),\n    ...(unconfirmedFacts.length > 0 ? [`发布前检查未通过：存在未确认事实：${unconfirmedFacts.join("、")}`] : []),\n  ];\n  return {\n    enterprisePositioningConsistent,\n    productDescriptionConsistent,\n    competitorDifferenceConsistent,\n    usesNonPublicAsset,\n    forbiddenTerms: unique(forbiddenTerms),\n    forbiddenClaims,\n    unconfirmedFacts,\n    blocked: blockReasons.length > 0,\n    blockReasons,\n    summary: blockReasons.length > 0 ? `发布前检查阻断：${blockReasons.join("；")}` : "发布前检查通过：企业定位、产品说明、竞品差异、公开资料和合规规则均未发现阻断项。",\n  };\n}\n`);

replaceOnce(files.logic, `export function buildGenerationBasis(input: { project: P11ProjectLike; topic: P11TopicDraft & { id?: number }; task: P11TaskLike; questions: P11QuestionLike[]; analyses: P11AnalysisLike[] }): P11GenerationBasis {`, `export function buildGenerationBasis(input: { project: P11ProjectLike; topic: P11TopicDraft & { id?: number }; task: P11TaskLike; questions: P11QuestionLike[]; analyses: P11AnalysisLike[]; assetLibrary?: P12AssetLibraryContext | null }): P11GenerationBasis {`);

replaceOnce(files.logic, `    manualReviewConclusion: manualReviewConclusion || "人工修订结论未单独补充，当前文章仅使用系统诊断结果，发布前建议业务负责人复核。",\n  };`, `    manualReviewConclusion: manualReviewConclusion || "人工修订结论未单独补充，当前文章仅使用系统诊断结果，发布前建议业务负责人复核。",\n    assetLibraryUsage: buildAssetLibraryUsage(input.assetLibrary),\n  };`);

replaceOnce(files.logic, `function formatGenerationBasis(basis: P11GenerationBasis) {\n  return [\n    \\`- 客户指定问题：\\${basis.customerQuestion}\\`,\n    \\`- 内容缺口：\\${basis.contentGap}\\`,\n    \\`- 优化任务：\\${basis.optimizationTask}\\`,\n    \\`- AI 未推荐原因：\\${basis.notRecommendedReason}\\`,\n    \\`- 竞品差距：\\${basis.competitorGap}\\`,\n  ].join("\\n");\n}\n`, `function formatGenerationBasis(basis: P11GenerationBasis) {\n  const usage = basis.assetLibraryUsage;\n  return [\n    \\`- 客户指定问题：\\${basis.customerQuestion}\\`,\n    \\`- 内容缺口：\\${basis.contentGap}\\`,\n    \\`- 优化任务：\\${basis.optimizationTask}\\`,\n    \\`- AI 未推荐原因：\\${basis.notRecommendedReason}\\`,\n    \\`- 竞品差距：\\${basis.competitorGap}\\`,\n    ...(usage ? [\n      "- 使用企业资料：\\n" + formatCitationList(usage.enterpriseMaterials, "暂无可用企业资料，需补充企业基础资料或产品服务资料。"),\n      "- 使用竞品资料：\\n" + (usage.competitorMaterials.length > 0 ? usage.competitorMaterials.map(item => \\`- \\${item.competitorName}；我方差异化：\\${item.differentiation || "待补充"}；来源：\\${item.sourceNotes || "资产库竞品资料"}\\`).join("\\n") : "暂无可引用竞品资料。"),\n      \\`- 是否使用客户案例：\\${usage.customerCaseUsage.status}\\`,\n      \\`- 是否使用合规规则：\\${usage.complianceRules.length > 0 ? usage.complianceRules.join("；") : "未配置，发布前需人工复核。"}\\`,\n      \\`- 是否使用内容风格：\\${usage.contentStyles.length > 0 ? usage.contentStyles.join("；") : "未配置，按稳健解释型风格处理。"}\\`,\n      \\`- 是否使用发布策略：\\${usage.publishStrategy.length > 0 ? usage.publishStrategy.join("；") : "未配置，默认全人工审核。"}\\`,\n      \\`- 证据缺口提示：\\${usage.missingEvidenceNotes.length > 0 ? usage.missingEvidenceNotes.join("；") : "暂无关键证据缺口。"}\\`,\n    ] : []),\n  ].join("\\n");\n}\n`);

replaceOnce(files.logic, `  analyses: P11AnalysisLike[];\n}): P11ArticleDraft {`, `  analyses: P11AnalysisLike[];\n  assetLibrary?: P12AssetLibraryContext | null;\n}): P11ArticleDraft {`);

replaceOnce(files.logic, `  const basis = buildGenerationBasis(input);`, `  const basis = buildGenerationBasis(input);`);
// The call above already passes the full input object, including assetLibrary.

replaceOnce(files.logic, `    paragraph("生成依据", formatGenerationBasis(basis)),`, `    paragraph("生成依据", formatGenerationBasis(basis)),\n    paragraph("资产库引用与证据边界", [\n      \\`使用企业资料：\\${basis.assetLibraryUsage?.enterpriseMaterials.length ? basis.assetLibraryUsage.enterpriseMaterials.map(item => item.title).join("、") : "暂无可用企业资料"}\\`,\n      \\`使用竞品资料：\\${basis.assetLibraryUsage?.competitorMaterials.length ? basis.assetLibraryUsage.competitorMaterials.map(item => item.competitorName).join("、") : "暂无可引用竞品资料"}\\`,\n      \\`是否使用客户案例：\\${basis.assetLibraryUsage?.customerCaseUsage.status ?? "案例信息待补充"}\\`,\n      \\`是否使用合规规则：\\${basis.assetLibraryUsage?.complianceRules.length ? "是" : "否，需人工复核"}\\`,\n      \\`是否使用内容风格：\\${basis.assetLibraryUsage?.contentStyles.length ? "是" : "否，采用稳健解释型风格"}\\`,\n      \\`是否使用发布策略：\\${basis.assetLibraryUsage?.publishStrategy.length ? "是" : "否，默认全人工审核"}\\`,\n      \\`证据边界：\\${basis.assetLibraryUsage?.missingEvidenceNotes.length ? basis.assetLibraryUsage.missingEvidenceNotes.join("；") : "暂无关键证据缺口"}\\`,\n    ].join("\\n")),`);

replaceOnce(files.logic, `    paragraph("六、发布前应补齐的证据清单", \\`为避免文章停留在概念层面，建议发布前由业务负责人补充以下真实证据：第一，能够证明\\${project.enterpriseName}服务边界的官网页面或产品说明；第二，能够解释\\${project.targetCustomers}为何适用的真实问答；第三，能够展示部署方式、售后流程或数据口径的截图；第四，与\\${basis.competitorNames.slice(0, 2).join("、")}进行客观比较时使用的可核验事实。若这些证据暂时缺失，应在文章中明确标注“需要补充”，而不是填写未经核验的案例或引用无法访问的链接。\\`),`, `    paragraph("六、发布前应补齐的证据清单", \\`为避免文章停留在概念层面，建议发布前由业务负责人补充以下真实证据：第一，能够证明\\${project.enterpriseName}服务边界的官网页面或产品说明；第二，能够解释\\${project.targetCustomers}为何适用的真实问答；第三，能够展示部署方式、售后流程或数据口径的截图；第四，与\\${basis.competitorNames.slice(0, 2).join("、")}进行客观比较时使用的可核验事实。若这些证据暂时缺失，应在文章中明确标注“需要补充”，而不是填写未经核验的案例或引用无法访问的链接。当前资产库证据边界：\\${basis.assetLibraryUsage?.missingEvidenceNotes.length ? basis.assetLibraryUsage.missingEvidenceNotes.join("；") : "暂无关键证据缺口"}。\\`),`);

replaceOnce(files.logic, `  task?: P11TaskLike | null;\n}): P11QualityScore {`, `  task?: P11TaskLike | null;\n  assetLibrary?: P12AssetLibraryContext | null;\n}): P11QualityScore {`);

replaceOnce(files.logic, `  const basisComplete = Boolean(input.article.generationBasis && validateGeoCollectableStructure(content, input.article.citableSnippets ?? undefined, input.article.generationBasis).filter(item => item === "完整生成依据").length === 0);\n\n  const problemMatchScore`, `  const basisComplete = Boolean(input.article.generationBasis && validateGeoCollectableStructure(content, input.article.citableSnippets ?? undefined, input.article.generationBasis).filter(item => item === "完整生成依据").length === 0);\n  const assetUsage = input.article.generationBasis?.assetLibraryUsage ?? buildAssetLibraryUsage(input.assetLibrary);\n  const assetEvidenceStrength = assetUsage.enterpriseMaterials.length > 0 && assetUsage.competitorMaterials.length > 0 && assetUsage.complianceRules.length > 0 ? "高" : assetUsage.enterpriseMaterials.length > 0 || assetUsage.competitorMaterials.length > 0 ? "中" : "低";\n  const prePublishCheck = evaluateAssetLibraryPrePublishCheck({ content, project: input.project, basis: input.article.generationBasis ?? null, assetLibrary: input.assetLibrary });\n\n  const problemMatchScore`);

replaceOnce(files.logic, `  const evidenceScore = Math.min(20, 6 + Math.min(gapMatches, 2) * 4 + Math.min(competitorMatches, 2) * 2 + (input.task ? 4 : 0) + (basisComplete ? 2 : 0));`, `  const evidenceScore = Math.min(20, 4 + Math.min(gapMatches, 2) * 3 + Math.min(competitorMatches, 2) * 2 + (input.task ? 3 : 0) + (basisComplete ? 2 : 0) + (assetEvidenceStrength === "高" ? 5 : assetEvidenceStrength === "中" ? 3 : 0));`);

replaceOnce(files.logic, `    ...forbiddenReasons,\n    ...(structureBlocked ? [\`缺少 GEO 可收录结构或生成依据：\${structureIssues.join("、")}\`] : []),`, `    ...forbiddenReasons,\n    ...prePublishCheck.blockReasons,\n    ...(structureBlocked ? [\`缺少 GEO 可收录结构或生成依据：\${structureIssues.join("、")}\`] : []),`);

replaceOnce(files.logic, `    ...(forbiddenReasons.length > 0 ? ["删除假链接、占位链接、虚假案例、虚假数据和排名保证等高风险表述。"] : []),\n  ];`, `    ...(forbiddenReasons.length > 0 || prePublishCheck.blocked ? ["删除假链接、占位链接、虚假案例、虚假数据、排名保证、禁用词、不可公开资料引用等高风险表述。"] : []),\n    ...(assetEvidenceStrength === "低" ? ["补充并人工确认企业资料、竞品资料、合规规则和发布策略，提升资产库证据强度。"] : []),\n  ];`);

replaceOnce(files.logic, `      ? \`质检未通过：\${blockReasons.join("；")}。优化建议：\${optimizationSuggestions.join("；")}\`\n      : \`质检通过：文章具备生成依据、GEO 可收录结构、引用友好片段、平台适配素材和合规说明，质量分 \${totalScore}。优化建议：\${optimizationSuggestions.join("；")}\`,`, `      ? \`质检未通过：资产库证据强度：\${assetEvidenceStrength}；事实来源：\${assetUsage.enterpriseMaterials.length + assetUsage.competitorMaterials.length + assetUsage.customerCaseUsage.references.length} 条资产库引用；未确认事实：\${prePublishCheck.unconfirmedFacts.length ? prePublishCheck.unconfirmedFacts.join("、") : "无"}；合规风险：\${prePublishCheck.blockReasons.length ? prePublishCheck.blockReasons.join("、") : "无"}。阻断原因：\${blockReasons.join("；")}。优化建议：\${optimizationSuggestions.join("；")}\`\n      : \`质检通过：资产库证据强度：\${assetEvidenceStrength}；事实来源：\${assetUsage.enterpriseMaterials.length + assetUsage.competitorMaterials.length + assetUsage.customerCaseUsage.references.length} 条资产库引用；未确认事实：\${prePublishCheck.unconfirmedFacts.length ? prePublishCheck.unconfirmedFacts.join("、") : "无"}；合规风险：\${prePublishCheck.blockReasons.length ? prePublishCheck.blockReasons.join("、") : "无"}。文章具备生成依据、GEO 可收录结构、引用友好片段、平台适配素材和合规说明，质量分 \${totalScore}。优化建议：\${optimizationSuggestions.join("；")}\`,`);

// routers.ts: import and collect asset context.
replaceOnce(files.routers, `  generateGeoArticleDraft,\n  generateGeoArticleTopics,\n  scoreGeoArticleQuality,\n`, `  evaluateAssetLibraryPrePublishCheck,\n  generateGeoArticleDraft,\n  generateGeoArticleTopics,\n  scoreGeoArticleQuality,\n`);

insertAfter(files.routers, `async function getProjectOrThrow(projectId: number) {\n  const db = await requireDb();\n  const result = await db.select().from(projects).where(eq(projects.id, projectId)).limit(1);\n  if (result.length === 0) throw new TRPCError({ code: "NOT_FOUND", message: "项目不存在" });\n  return result[0];\n}\n`, `\nasync function getAssetLibraryContextForProject(projectId: number) {\n  const db = await requireDb();\n  const [profileRows, assetRows, caseRows, competitorRows, complianceRows, styleRows, strategyRows] = await Promise.all([\n    db.select().from(enterpriseGeoProfiles).where(eq(enterpriseGeoProfiles.projectId, projectId)).orderBy(desc(enterpriseGeoProfiles.updatedAt)).limit(1),\n    db.select().from(geoAssetSources).where(eq(geoAssetSources.projectId, projectId)).orderBy(desc(geoAssetSources.createdAt)),\n    db.select().from(customerCases).where(eq(customerCases.projectId, projectId)).orderBy(desc(customerCases.updatedAt)),\n    db.select().from(competitorProfiles).where(eq(competitorProfiles.projectId, projectId)).orderBy(desc(competitorProfiles.updatedAt)),\n    db.select().from(complianceRules).where(eq(complianceRules.projectId, projectId)).orderBy(desc(complianceRules.updatedAt)),\n    db.select().from(contentStyleProfiles).where(eq(contentStyleProfiles.projectId, projectId)).orderBy(desc(contentStyleProfiles.updatedAt)),\n    db.select().from(publishStrategies).where(eq(publishStrategies.projectId, projectId)).orderBy(desc(publishStrategies.updatedAt)),\n  ]);\n  return {\n    profile: profileRows[0] ?? null,\n    assetSources: assetRows,\n    customerCases: caseRows,\n    competitorProfiles: competitorRows,\n    complianceRules: complianceRows,\n    contentStyleProfiles: styleRows,\n    publishStrategies: strategyRows,\n  };\n}\n`);

replaceOnce(files.routers, `      const draft = generateGeoArticleDraft({\n        project,\n        topic: { ...topic, id: topic.id, articleType: topic.articleType as typeof articleTypes[number], optimizationTaskId: task.id },\n        task,\n        questions: questionScope.length > 0 ? questionScope : projectQuestions,\n        analyses: analysisScope.length > 0 ? analysisScope : analysesWithQuestions,\n      });`, `      const assetLibrary = await getAssetLibraryContextForProject(topic.projectId);\n      const draft = generateGeoArticleDraft({\n        project,\n        topic: { ...topic, id: topic.id, articleType: topic.articleType as typeof articleTypes[number], optimizationTaskId: task.id },\n        task,\n        questions: questionScope.length > 0 ? questionScope : projectQuestions,\n        analyses: analysisScope.length > 0 ? analysisScope : analysesWithQuestions,\n        assetLibrary,\n      });`);

replaceOnce(files.routers, `      const quality = scoreGeoArticleQuality({\n        article: article as unknown as Parameters<typeof scoreGeoArticleQuality>[0]["article"],\n        project,\n        questions: projectQuestions,\n        analyses: analysesWithQuestions,\n        task: taskRows[0] ?? null,\n      });`, `      const assetLibrary = await getAssetLibraryContextForProject(article.projectId);\n      const quality = scoreGeoArticleQuality({\n        article: article as unknown as Parameters<typeof scoreGeoArticleQuality>[0]["article"],\n        project,\n        questions: projectQuestions,\n        analyses: analysesWithQuestions,\n        task: taskRows[0] ?? null,\n        assetLibrary,\n      });`);

replaceOnce(files.routers, `      if (!latestScore || latestScore.blocked || latestScore.totalScore < 80) throw new TRPCError({ code: "BAD_REQUEST", message: "文章质量分低于 80 或存在禁止发布风险，不能发布" });\n      const publicPath =`, `      if (!latestScore || latestScore.blocked || latestScore.totalScore < 80) throw new TRPCError({ code: "BAD_REQUEST", message: "文章质量分低于 80 或存在禁止发布风险，不能发布" });\n      const project = await getProjectOrThrow(article.projectId);\n      const assetLibrary = await getAssetLibraryContextForProject(article.projectId);\n      const prePublishCheck = evaluateAssetLibraryPrePublishCheck({\n        content: \\`\\${article.title}\\n\\${article.markdownContent}\\`,\n        project,\n        basis: article.generationBasis as Parameters<typeof evaluateAssetLibraryPrePublishCheck>[0]["basis"],\n        assetLibrary,\n      });\n      if (prePublishCheck.blocked) {\n        throw new TRPCError({ code: "BAD_REQUEST", message: prePublishCheck.summary });\n      }\n      const publicPath =`);

replaceOnce(files.routers, `        notes: "人工审核通过后发布到系统内置 GEO 内容页，等待复测。",`, `        notes: "人工审核通过后发布到系统内置 GEO 内容页；发布前检查已读取企业资产库并通过，等待复测。",`);

// GeoPages.tsx: broaden generation basis view and rows.
replaceOnce(files.geoPages, `type ArticleGenerationBasisView = {\n  customerQuestion?: string;\n  contentGap?: string;\n  optimizationTaskName?: string;\n  notRecommendedReason?: string;\n  competitorGap?: string;\n  humanRevisionConclusion?: string;\n};`, `type ArticleGenerationBasisView = {\n  customerQuestion?: string;\n  contentGap?: string;\n  optimizationTaskName?: string;\n  optimizationTask?: string;\n  notRecommendedReason?: string;\n  competitorGap?: string;\n  humanRevisionConclusion?: string;\n  manualReviewConclusion?: string;\n  assetLibraryUsage?: {\n    enterpriseMaterials?: Array<{ title?: string; trustLevel?: string; isPublic?: boolean }>;\n    competitorMaterials?: Array<{ competitorName?: string; differentiation?: string | null }>;\n    customerCaseUsage?: { used?: boolean; status?: string; references?: Array<{ customerName?: string }> };\n    complianceRules?: string[];\n    contentStyles?: string[];\n    publishStrategy?: string[];\n    missingEvidenceNotes?: string[];\n  };\n};`);

replaceOnce(files.geoPages, `    ["优化任务", basis.optimizationTaskName ?? ""],\n    ["AI 未推荐原因", basis.notRecommendedReason ?? ""],\n    ["竞品差距", basis.competitorGap ?? ""],\n    ["人工修订结论", basis.humanRevisionConclusion ?? ""],`, `    ["优化任务", basis.optimizationTaskName ?? basis.optimizationTask ?? ""],\n    ["AI 未推荐原因", basis.notRecommendedReason ?? ""],\n    ["竞品差距", basis.competitorGap ?? ""],\n    ["使用企业资料", basis.assetLibraryUsage?.enterpriseMaterials?.map(item => \\`${item.title ?? "未命名资料"}（\\${item.trustLevel ?? "可信度未标记"}，\\${item.isPublic ? "可公开" : "不可公开"}）\\`).join("；") ?? ""],\n    ["使用竞品资料", basis.assetLibraryUsage?.competitorMaterials?.map(item => \\`${item.competitorName ?? "未命名竞品"}：\\${item.differentiation ?? "差异化待补充"}\\`).join("；") ?? ""],\n    ["是否使用客户案例", basis.assetLibraryUsage?.customerCaseUsage?.status ?? ""],\n    ["是否使用合规规则", basis.assetLibraryUsage?.complianceRules?.join("；") ?? ""],\n    ["是否使用内容风格", basis.assetLibraryUsage?.contentStyles?.join("；") ?? ""],\n    ["是否使用发布策略", basis.assetLibraryUsage?.publishStrategy?.join("；") ?? ""],\n    ["证据缺口提示", basis.assetLibraryUsage?.missingEvidenceNotes?.join("；") ?? ""],\n    ["人工修订结论", basis.humanRevisionConclusion ?? basis.manualReviewConclusion ?? ""],`);

// Public content page uses a similar compact renderer.
replaceOnce(files.publicContent, `type GenerationBasisView = {\n  customerQuestion?: string;\n  contentGap?: string;\n  optimizationTaskName?: string;\n  notRecommendedReason?: string;\n  competitorGap?: string;\n  humanRevisionConclusion?: string;\n};`, `type GenerationBasisView = {\n  customerQuestion?: string;\n  contentGap?: string;\n  optimizationTaskName?: string;\n  optimizationTask?: string;\n  notRecommendedReason?: string;\n  competitorGap?: string;\n  humanRevisionConclusion?: string;\n  manualReviewConclusion?: string;\n  assetLibraryUsage?: {\n    enterpriseMaterials?: Array<{ title?: string; trustLevel?: string; isPublic?: boolean }>;\n    competitorMaterials?: Array<{ competitorName?: string; differentiation?: string | null }>;\n    customerCaseUsage?: { status?: string };\n    complianceRules?: string[];\n    contentStyles?: string[];\n    publishStrategy?: string[];\n    missingEvidenceNotes?: string[];\n  };\n};`);

replaceOnce(files.publicContent, `    ["优化任务", basis.optimizationTaskName ?? ""],\n    ["AI 未推荐原因", basis.notRecommendedReason ?? ""],\n    ["竞品差距", basis.competitorGap ?? ""],\n    ["人工修订结论", basis.humanRevisionConclusion ?? ""],`, `    ["优化任务", basis.optimizationTaskName ?? basis.optimizationTask ?? ""],\n    ["AI 未推荐原因", basis.notRecommendedReason ?? ""],\n    ["竞品差距", basis.competitorGap ?? ""],\n    ["使用企业资料", basis.assetLibraryUsage?.enterpriseMaterials?.map(item => \\`${item.title ?? "未命名资料"}（\\${item.trustLevel ?? "可信度未标记"}，\\${item.isPublic ? "可公开" : "不可公开"}）\\`).join("；") ?? ""],\n    ["使用竞品资料", basis.assetLibraryUsage?.competitorMaterials?.map(item => \\`${item.competitorName ?? "未命名竞品"}：\\${item.differentiation ?? "差异化待补充"}\\`).join("；") ?? ""],\n    ["是否使用客户案例", basis.assetLibraryUsage?.customerCaseUsage?.status ?? ""],\n    ["是否使用合规规则", basis.assetLibraryUsage?.complianceRules?.join("；") ?? ""],\n    ["是否使用内容风格", basis.assetLibraryUsage?.contentStyles?.join("；") ?? ""],\n    ["是否使用发布策略", basis.assetLibraryUsage?.publishStrategy?.join("；") ?? ""],\n    ["证据缺口提示", basis.assetLibraryUsage?.missingEvidenceNotes?.join("；") ?? ""],\n    ["人工修订结论", basis.humanRevisionConclusion ?? basis.manualReviewConclusion ?? ""],`);

// Tests: append focused Sprint 1.5 logic tests.
insertAfter(files.test, `});\n`, `\ndescribe("V1.2 Sprint 1.5 资产库接入验证", () => {\n  const dolphinAssetLibrary = {\n    profile: {\n      enterpriseName: "海豚知道",\n      targetCustomers: "希望通过 AI 搜索获得稳定曝光的 B2B 企业",\n      productServiceIntro: "企业 AI 搜索增长与 GEO 内容资产管理服务",\n      productIntro: "GEO 诊断、内容生成、质量评分与发布前检查",\n      servicePriceRange: "",\n      priceExplanation: "",\n    },\n    assetSources: [\n      { id: 1, title: "海豚知道企业基础资料", sourceType: "企业基础资料", trustLevel: "高", isPublic: 1, canUseForGeneration: 1, manuallyConfirmed: 1, structuredSummary: { digest: "海豚知道面向 B2B 企业提供 AI 搜索增长工作台。" }, contentDigest: "海豚知道企业定位" },\n      { id: 2, title: "海豚知道产品服务资料", sourceType: "产品服务资料", trustLevel: "高", isPublic: 1, canUseForGeneration: 1, manuallyConfirmed: 1, structuredSummary: { digest: "包含 GEO 诊断、文章生成、质量评分、发布前检查。" }, contentDigest: "产品服务说明" },\n    ],\n    customerCases: [],\n    competitorProfiles: [\n      { id: 1, competitorName: "传统 SEO 代运营", officialWebsite: "https://example.org", differentiation: "海豚知道强调资产库证据、合规检查和 AI 引用结构。", canReference: 1, sourceNotes: "客户录入竞品资料" },\n    ],\n    complianceRules: [\n      { id: 1, enabled: 1, ruleName: "AI 搜索内容合规", brandTone: "专业、克制、基于证据", forbiddenWords: "稳赚,第一名", sensitiveWords: "", forbiddenClaims: "保证收录,保证排名", specialComplianceNotes: "不得承诺平台排名。" },\n    ],\n    contentStyleProfiles: [\n      { id: 1, enabled: 1, profileName: "海豚知道解释型风格", brandTone: "专业可信", styleDescription: "先给结论，再说明依据和边界。" },\n    ],\n    publishStrategies: [\n      { id: 1, enabled: 1, publishMode: "保守", reviewMode: "全人工审核", minQualityScore: 80, priorityPlatforms: ["官网版", "知乎回答版"] },\n    ],\n  };\n\n  it("生成海豚知道文章时展示资产库引用并保留缺失证据提示", () => {\n    const draft = generateGeoArticleDraft({\n      project: { ...project, enterpriseName: "海豚知道", productIntro: "GEO 诊断、内容生成、质量评分与发布前检查", targetCustomers: "希望通过 AI 搜索获得稳定曝光的 B2B 企业" },\n      topic: { ...topic, title: "海豚知道如何把企业资料变成 AI 可引用内容" },\n      task,\n      questions,\n      analyses,\n      assetLibrary: dolphinAssetLibrary,\n    });\n\n    expect(draft.generationBasis.assetLibraryUsage?.enterpriseMaterials.map(item => item.title)).toContain("海豚知道企业基础资料");\n    expect(draft.generationBasis.assetLibraryUsage?.competitorMaterials[0]?.competitorName).toBe("传统 SEO 代运营");\n    expect(draft.markdownContent).toContain("使用企业资料");\n    expect(draft.markdownContent).toContain("使用竞品资料");\n    expect(draft.markdownContent).toContain("案例信息待补充");\n    expect(draft.markdownContent).toContain("数据暂无公开来源");\n    expect(draft.markdownContent).toContain("价格口径需客户确认");\n  });\n\n  it("质量评分读取资产库证据强度、事实来源和合规风险", () => {\n    const draft = generateGeoArticleDraft({ project: { ...project, enterpriseName: "海豚知道" }, topic, task, questions, analyses, assetLibrary: dolphinAssetLibrary });\n    const quality = scoreGeoArticleQuality({\n      article: draft,\n      project: { ...project, enterpriseName: "海豚知道" },\n      questions,\n      analyses,\n      task,\n      assetLibrary: dolphinAssetLibrary,\n    });\n\n    expect(quality.reviewSummary).toContain("资产库证据强度");\n    expect(quality.reviewSummary).toContain("事实来源");\n    expect(quality.reviewSummary).toContain("未确认事实");\n    expect(quality.reviewSummary).toContain("合规风险");\n  });\n\n  it("发布前检查读取资产库并阻断不可公开资料、禁用词和保证排名承诺", () => {\n    const draft = generateGeoArticleDraft({ project: { ...project, enterpriseName: "海豚知道" }, topic, task, questions, analyses, assetLibrary: { ...dolphinAssetLibrary, assetSources: [{ ...dolphinAssetLibrary.assetSources[0], isPublic: 0 }] } });\n    const check = evaluateAssetLibraryPrePublishCheck({\n      content: `${draft.title}\\n${draft.markdownContent}\\n保证排名 第一名`,\n      project: { ...project, enterpriseName: "海豚知道" },\n      basis: draft.generationBasis,\n      assetLibrary: { ...dolphinAssetLibrary, assetSources: [{ ...dolphinAssetLibrary.assetSources[0], isPublic: 0 }] },\n    });\n\n    expect(check.blocked).toBe(true);\n    expect(check.blockReasons.join("；")).toContain("不可公开资料");\n    expect(check.blockReasons.join("；")).toContain("禁用词");\n    expect(check.blockReasons.join("；")).toContain("保证收录或排名");\n  });\n});\n`);
