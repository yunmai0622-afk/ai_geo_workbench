# GEO系统（集星云推）功能分析 & Cursor 实现指令

> 分析日期：2026-05-23  
> 系统名称：集星云推 · 企业级GEO搜索优化一站式平台  
> 分析范围：代理端 + 商家端全功能模块

---

## 一、系统整体架构

这是一个典型的 **SaaS + 代理商** 三层架构：

```
平台方（集星科技）
  └── 代理商（算力购买 → 分配给商家）
        └── 商家（使用GEO功能，提升AI搜索排名）
```

核心商业逻辑：**算力** 是货币单位。代理商充值算力，开通商户时扣除算力，商家用算力驱动AI生产内容。

---

## 二、代理端功能清单（geo.jixingzhijian.com/dydqt）

### 2.1 控制台（Dashboard）
- 顶部 Banner：品牌宣传 + 使用教程链接
- 账户信息卡：登录账号、到期时间、**客户端下载**按钮
- 5步引导流程：算力充值 → 开户套餐 → 开通商户 → 网站配置 → 矣岩认证
- 矣岩认证次数显示（当前可用N次）
- 核心统计卡片（6个）：开通商户数、文章生成数、文章发布数、发布媒体数、集成网站数、自动策略数
- 图表区：平台总发布（饼图：媒体/网站分布）+ 文章发布频次（折线图，7/15/30天切换）

### 2.2 商户管理
**开户套餐**
- 套餐列表：套餐名 / 包含算力 / 分配空间 / 有效期（月）/ 子账号数 / 售价 / 创建时间
- 添加套餐、按时间/名称搜索、编辑/删除

**商户列表**
- 商家信息：ID / 头像 / 名称 / 子账号授权数（可修改）
- 登录账号 / SUID / 状态（开启/封禁）
- 算力：已用 / 剩余 / 分配算力入口
- 来源备注（渠道追踪）
- 时间：开通 / 活跃 / 到期
- 认证信息：已/未认证 + 可用认证次数
- 操作：**一键登录**（免密进入商家后台）/ 编辑商户 / 存储配置
- 批量操作 / 筛选 / 刷新

### 2.3 账务管理
**收支明细**
- 账户类型（认证/算力）、收支类型（开通商户/平台充值）、收支状态（支出红/收入绿）
- 收支描述、交易数量、收支时间

**算力流水**
- 显示当前剩余算力
- 记录：类型（分配/充值）/ 调整数量 / 调整时间 / 备注

### 2.4 合伙人管理
- 合伙人列表（目前无数据）
- 添加合伙人功能

### 2.5 账号管理
- 业务账号管理

### 2.6 应用管理
- 网站配置

---

## 三、商家端功能清单（geo.jixingzhijian.com/geo/console/index）

### 3.1 工作台（Dashboard）
- 账户卡片：商家名称 / 到期时间标签 / 已认证标识 / 算力统计（已用/总量）/ 存储空间
- 6步引导（可点击）：①蒸馏词 → ②上传图库 → ③AI指令 → ④AI创作 → ⑤授权账号 → ⑥发布任务
- 4个功能入口卡片：AI可见度诊断 / AI智能模板中心 / AI内容智创 / 官媒投稿发文
- 最新发布文章列表
- 热门问题词收录（各AI平台收录量）

### 3.2 AI训练（核心模块）
**蒸馏词库**
- 左侧：主词/核心词分类树（可添加分组）
- 右侧：问题词列表
  - 列：问题词内容 / 收录状态（未收录/已收录）/ 收录平台 / 查询收录按钮 / 权重 / 训练时间 / 意向阶段（正面词/负面词）/ 审核状态 / 操作
- 批量操作：批量训练 / 批量编辑收录状态 / 批量删除
- 录入问题词 / 组合问题词

**知识库**
- 企业知识库：名称 / 类型（普通/高级知识）/ 内容预览
- 创建知识库 / 编辑 / 删除

**图片素材库**
- 素材分组管理
- 上传图片、快速设置分组、批量删除
- 存储空间显示（0B/3GB），支持扩容

### 3.3 账号管理
**发布媒体（授权媒体账号）**
- 支持平台（已验证）：搜狐号 / 头条号 / 网易号 / 百家号 / 公众号 / 抖音 / 小红书 + 更多
- 分组管理（默认分组 + 自定义）
- 账号列表：昵称 / 头像 / 平台 / 授权状态 / 授权时间 / 备注
- 新的授权 / 批量修改分组 / 批量删除

**媒体管理**（标注"开发中"）

### 3.4 AI创作

**一键创作（图文创作一键成文）**
- 交互式模板句："我是一个 [平台▼] 博主，帮我生成一篇关于 [主题▼] 的文章！"
- 插入知识库内容（下拉选择 + 获取内容按钮）
- 插入AI指令内容（下拉选择 + 获取内容按钮）
- 插入图片（开关）
- 一键创作按钮

**创作指令**
- AI指令列表：名称 / 来源 / 状态筛选
- 创建AI指令 / 批量删除

**文章列表**
- 文章库分组管理
- 已通过AI指令创作的文章列表
- 创作文章 / 设置分组 / 批量删除

**开始创作**
- 创建文章创作计划及执行记录
- 批量创作文章（立即执行）
- 创建自动创作（计划执行）
- 列表：任务名称 / 任务类型 / 执行状态 / 筛选

**自动创作**
- 统计面板：自动创作任务数 / 已执行创作任务 / 待执行创作任务 / 累计创作文章
- 创作控制：开始执行 / 停止
- 创作配置：是否显示浏览器（开关）/ 任务启动间隔（秒）/ 保存配置
- 实时执行日志（全部/info/error过滤）

### 3.5 自动化发布

**发布计划及任务**
- 创建自动发布计划
- 任务列表：名称 / 任务状态筛选 / 查询 / 刷新

**发布执行记录**
- 文章发布的流水式记录
- 按媒体账号名称搜索

**开始发布任务**（独立页面，类似自动创作的控制面板）

### 3.6 收录排名

**收录大模型（AI大模型授权）**
- 国内AI平台：豆包 / deepseek / 文心一言 / 腾讯元宝 / 通义千问 / KIMI / 纳米AI
- 海外AI平台：ChatGPT / Gemini / Claude / Perplexity / Grok
- 每个平台显示"已授权/未授权"状态
- 授权列表

**创建收录任务**
- 问题词收录计划及任务
- 创建收录自动查询（计划）
- 任务状态筛选

**执行收录任务**
- 收录统计：蒸馏词库数 / 问题词总量 / 收录总量 / 待查询问题词 / 收录报表数
- 收录控制：开始执行 / 停止
- 收录配置：是否显示浏览器 / 任务间隔
- 实时执行日志

**收录报表视图**（数据可视化）

### 3.7 付费投稿

**官方媒体**
- 多维度筛选：
  - 地区（全国各省市 + 港澳台）
  - 频道类型（IT科技/生活消费/女性时尚/娱乐/教育/健康医疗/房产/财经等20+）
  - 综合门户（央视网/人民网/凤凰网/腾讯网/搜狐/新浪/新华网等30+）
  - 新闻源（百度新闻源/非新闻源）
  - 收录情况（不包收录/百度包收录）
  - 入口级别（首页入口/频道入口/上级入口）
- 媒体表格：名称/链接 / 价格 / 出稿率 / 收录率 / 电脑权重 / 移动权重 / 备注
- 批量选择 + 付款流程（可用余额/充值）

**官方自媒体**（同类筛选）

**海外媒体**

**投稿记录**

---

## 四、亮点分析（值得借鉴）

| # | 功能 | 亮点点评 |
|---|------|---------|
| 1 | **算力货币化** | 用"算力"统一量化服务消耗，代理商分配给商家，逻辑清晰，易于定价和控制 |
| 2 | **一键登录商户** | 代理商可免密直接进入客户后台，代运营场景极其实用 |
| 3 | **蒸馏词库 + 意向阶段** | 将传统SEO词库升级为GEO问题词，区分正面/负面意向，对AI收录有针对性 |
| 4 | **12个AI平台收录授权** | 一次性覆盖豆包/Deepseek/文心/GPT/Claude等主流AI，是核心壁垒 |
| 5 | **付费媒体库** | 按地区/频道/门户/权重多维筛选，覆盖全国媒体资源，可直接付费投稿 |
| 6 | **工作台6步引导** | 渐进式新手引导，每步可点击直达，降低上手门槛 |
| 7 | **知识库 + AI指令分离** | 知识库存企业资料，AI指令存写作规则，两者在创作时注入，架构干净 |
| 8 | **执行日志实时滚动** | 自动创作/收录任务的实时日志，增加透明感和信任感 |

---

## 五、问题与优化建议

| # | 问题 | 具体表现 | 优化建议 |
|---|------|---------|---------|
| 1 | **本地依赖严重** | 自动创作、收录任务、发布任务均需"保持当前页面开启"，依赖本地浏览器执行 | 改为云端服务器调度，用户关闭浏览器任务仍执行 |
| 2 | **算力单位模糊** | 用户不知道每篇文章/每次收录消耗多少算力 | 在操作前提示预计消耗，显示余量预警 |
| 3 | **缺乏效果数据** | 没有GEO效果大盘（收录率趋势、AI搜索曝光量、品牌词出现频次）| 增加数据效果看板，让商家看到投入产出 |
| 4 | **一键创作交互简陋** | 主题输入是纯文本框，无模板预览、无字数控制、无风格选择 | 增加内容模板卡片选择（图文/问答/长文），支持预览 |
| 5 | **两端登录分离** | 代理商和商家需要不同链接登录，体验割裂 | 统一登录入口，根据角色自动跳转 |
| 6 | **媒体授权流程不清** | 发布媒体的授权方式未说明（扫码？账密？） | 增加每个平台的授权说明文档/视频引导 |
| 7 | **合伙人功能缺失** | 合伙人列表为空，功能未实现 | 补充合伙人分成机制和结算功能 |
| 8 | **移动端缺失** | 系统无响应式布局，手机无法使用 | 增加移动端数据查看页面（至少dashbaord） |
| 9 | **批量操作体验差** | 批量选择后操作入口分散 | 顶部固定批量操作栏，统一管理 |
| 10 | **无消息通知** | 任务执行完成无推送通知 | 增加站内信/微信通知，任务完成及时告知 |

---

## 六、Cursor 实现指令（完整版）

> 以下是可以直接复制粘贴给 Cursor 的 Prompt，按模块分组

---

### 📦 技术栈建议（先告诉 Cursor）

```
技术栈：
- 前端：Vue 3 + Vite + TypeScript
- UI组件：Element Plus（或 Ant Design Vue）
- 图表：ECharts
- 状态管理：Pinia
- HTTP：Axios
- 后端：Node.js + Express（或 Python + FastAPI）
- 数据库：MySQL + Redis
- 任务调度：Bull（Node.js队列，替代本地浏览器执行）
- 文件存储：阿里云OSS（图片素材）
```

---

### 🏗️ PROMPT 1：项目脚手架

```
请帮我初始化一个 GEO 内容优化平台的前端项目，技术栈为 Vue 3 + TypeScript + Vite + Element Plus + Pinia + Vue Router + Axios + ECharts。

项目名：geo-platform
目录结构如下：
src/
├── api/          # 接口层，按模块分文件
├── assets/       # 静态资源
├── components/   # 公共组件
├── layouts/      # 布局组件（AdminLayout、AgentLayout）
├── router/       # 路由配置
├── stores/       # Pinia 状态管理
├── types/        # TypeScript 类型定义
├── utils/        # 工具函数
└── views/        # 页面
    ├── agent/    # 代理端页面
    └── merchant/ # 商家端页面

请创建基础的路由守卫，根据用户角色（agent/merchant）跳转到不同的 layout。
统一登录入口 /login，登录后根据 role 字段自动跳转。
```

---

### 🎨 PROMPT 2：整体布局

```
帮我创建 AdminLayout.vue，这是一个后台管理系统的基础布局组件，要求：

1. 左侧固定侧边栏（宽度 200px），深色主题（#1a1a2e 或深蓝色）
2. 顶部 Header（高度 60px），显示：系统 Logo（左）、账号名+手机号（中右）、通知铃铛、消息图标、设置图标
3. 内容区域：右侧主体，overflow-y: auto
4. 侧边菜单支持折叠/展开子菜单，当前选中项高亮
5. 菜单项有图标（使用 Element Plus 图标库）

商家端菜单结构（MerchantLayout）：
- 工作台
- AI训练
  - 蒸馏词库
  - 知识库
  - 图片素材库
- 账号管理
  - 发布媒体
  - 媒体管理
- AI创作
  - 一键创作
  - 创作指令
  - 文章列表
  - 开始创作
  - 自动创作
- 自动化发布
  - 发布计划及任务
  - 发布执行记录
  - 开始发布任务
- 收录排名
  - 收录大模型
  - 创建收录任务
  - 执行收录任务
  - 收录报表
- 付费投稿
  - 官方媒体
  - 官方自媒体
  - 海外媒体
  - 投稿记录

左下角显示：空间容量进度条 / 算力总计 / 账号到期时间
```

---

### 📊 PROMPT 3：商家端工作台

```
帮我创建商家端工作台页面 views/merchant/Dashboard.vue，包含以下模块：

1. 顶部账户卡片（右侧浮动）：
   - 商家头像（圆形）
   - 商家名称
   - 到期时间标签（如"2027-05-21到期"，橙色）
   - 已认证绿色标识
   - 算力统计：已用/总量（如 20/1000P）
   - 存储空间（如 0B/3G）

2. 6步新手引导（可点击跳转）：
   步骤号+步骤名，横向箭头连接，当前已完成步骤高亮
   ①蒸馏词 ②上传图库 ③AI指令 ④AI创作 ⑤授权账号 ⑥发布任务

3. 4个功能入口卡片（2x2 grid）：
   - AI可见度诊断（标签：智能诊断/洞察先机）
   - AI智能模板中心（标签：创作赋能/智慧大脑）
   - AI内容智创（标签：轻松撰稿/智创无限）
   - 官媒投稿发文（标签：官媒直通/能量加持）
   每个卡片有渐变背景色、图标、标题、简介、"立即体验"按钮

4. 底部两栏：
   - 最新发布文章列表（文章标题 + 发布时间 + 状态）
   - 热门问题词收录（问题词 + AI平台 + 收录数）

注意：整体风格偏科技感，紫蓝色渐变主色调，卡片有轻微阴影和 hover 动效。
```

---

### 🧠 PROMPT 4：蒸馏词库

```
帮我创建蒸馏词库页面 views/merchant/ai-training/KeywordLibrary.vue

页面分为左右两栏：

左栏（宽度 240px）：
- 标题"蒸馏词（主词/核心词）"
- 顶部有"+"按钮添加主词分组
- 列表显示所有主词，点击切换，选中项蓝色高亮

右栏：
- 顶部信息栏：目标达成词（显示当前主词相关的目标达成词）
- 操作按钮行：刷新 / 批量训练 / 录入问题词 / 组合问题词 / 批量编辑收录状态 / 批量删除
- 表格（支持多选）：
  | 问题词（搜索/收录/推荐） | 收录状态 | 收录平台 | 查询收录 | 权重 | 训练时间 | 意向阶段 | 审核状态 | 操作 |
  
  - 收录状态：绿色"已收录"/ 红色"未收录" tag
  - 收录平台：平台图标显示
  - 查询收录：按钮，点击触发查询
  - 权重：数字
  - 意向阶段：正面词/负面词 tag
  - 操作：编辑 / 删除

- 弹窗：录入问题词（支持手动输入 + 批量上传文件）

优化点（相比原系统）：
1. 增加"问题词预览"侧边抽屉，点击问题词可查看AI搜索引擎实际搜索结果
2. 增加收录率统计小图表（折线图，显示近7天收录变化）
3. 权重改为可视化星级评分显示
```

---

### 🤖 PROMPT 5：AI创作 - 一键创作

```
帮我创建一键创作页面 views/merchant/ai-creation/QuickCreate.vue

页面标题：图文创作一键成文
副标题：多平台风格选择，多角度切入热点，创作从未如此简单

主要内容区（居中，最大宽度 800px）：

1. 模板化输入句（交互式 Mad-Libs 风格）：
   "我是一个 [平台下拉] 博主，帮我生成一篇关于 [主题输入] 的文章！"
   - 平台下拉：搜狐/头条/小红书/微信公众号/抖音/百家号/网易号
   - 主题：文本输入框

2. 高级配置（折叠面板）：
   - 插入知识库内容：下拉选择知识库 + "获取内容"按钮（展示内容预览）
   - 插入AI创作指令：下拉选择指令 + "获取指令"按钮
   - 插入图片：开关，开启后选择素材库图片
   - 文章长度：短文(500字)/中文(1000字)/长文(2000字+) 单选
   - 文章风格：专业/轻松/营销/故事化 单选

3. 一键创作按钮（大，主色渐变）

4. 创作结果区（创作完成后显示）：
   - 文章标题 + 正文预览
   - 操作：复制 / 编辑 / 保存到文章库 / 直接发布

优化点：
- 原系统只有极简输入框，这里增加风格和长度控制
- 创作结果支持富文本编辑（集成 TipTap 或 Quill）
- 显示本次消耗算力数
```

---

### ⚙️ PROMPT 6：自动创作（云端版 - 核心改进）

```
帮我创建自动创作页面 views/merchant/ai-creation/AutoCreate.vue

⚠️ 关键设计改进：原系统依赖本地浏览器执行（需要保持页面开启），
我们改为云端任务队列（Bull + Redis），用户关闭页面任务继续执行。

页面结构：

1. 统计面板（4个卡片）：
   自动创作任务数 / 已执行任务数 / 待执行任务数 / 累计创作文章数

2. 任务控制区：
   - 状态显示："云端运行中 ✓"（绿色）或"已暂停"（灰色）
   - 按钮：开始执行 / 暂停 / 停止全部
   - ⚠️ 提示：任务在云端执行，关闭浏览器不影响

3. 创建任务表单（弹窗/侧边栏）：
   - 任务名称
   - 使用主词：下拉（来自蒸馏词库）
   - 使用知识库：多选
   - 使用AI指令：选择
   - 目标文章数：数字输入
   - 执行频率：立即/每天/每周/指定时间（cron）
   - 平台风格：多选（头条/搜狐等）

4. 任务列表：
   | 任务名称 | 类型 | 创作数/目标数 | 状态 | 创建时间 | 操作 |
   状态tag：运行中（蓝色动画）/ 已完成 / 已暂停 / 失败

5. 执行日志（实时 WebSocket 推送）：
   时间 + 级别(info/warn/error) + 日志内容
   支持按级别过滤，清空日志

后端接口设计：
POST /api/tasks/auto-create     # 创建任务
PUT  /api/tasks/:id/pause       # 暂停
PUT  /api/tasks/:id/resume      # 恢复
DELETE /api/tasks/:id           # 删除
GET  /api/tasks/:id/logs        # 获取日志（支持 SSE 流式推送）
```

---

### 🌐 PROMPT 7：收录大模型授权

```
帮我创建 AI大模型授权页面 views/merchant/indexing/AiPlatforms.vue

页面分两个区域：

区域1：选择国内AI平台
标题：选择AI平台（选择需要授权的AI平台）
卡片网格（5列），每个卡片包含：
- 平台 Logo 图标
- 平台名称
- 授权状态 tag：已授权（绿色）/ 未授权（红色）
- 点击卡片 → 跳转/弹窗进行授权

平台列表（国内）：豆包 / Deepseek / 文心一言 / 腾讯元宝 / 通义千问 / KIMI / 纳米AI

区域2：选择海外AI平台
平台列表（海外）：ChatGPT / Gemini / Claude / Perplexity / Grok

区域3：授权列表
| 用户信息 | 平台 | 授权时间 | 状态 | 操作 |

优化点：
1. 每个平台卡片 hover 显示"如何授权"说明（tooltip 或弹窗）
2. 增加"一键检测授权状态"按钮（批量检测）
3. 授权成功后显示该平台已收录的问题词数量
4. 增加收录效果小图：近7天各平台收录量对比（柱状图）
```

---

### 📰 PROMPT 8：付费投稿 - 官方媒体库

```
帮我创建付费投稿官方媒体库页面 views/merchant/contribution/OfficialMedia.vue

页面顶部：多维度筛选面板（可折叠）
- 地区：全国 + 各省市（按钮组，全部/北京/上海/广东...）
- 频道类型：IT科技/生活消费/女性时尚/娱乐/教育/健康/房产/财经/新闻/文化/食品等
- 综合门户：央视网/人民网/凤凰网/腾讯/搜狐/新浪/新华/千龙网等
- 新闻源：全部/百度新闻源/非新闻源
- 收录情况：全部/不包收录/百度包收录
- 入口级别：全部/首页入口/频道入口/上级入口
- "更多选项"折叠展开按钮

右上角：媒体名称搜索框 + 批量检索

媒体列表（表格+卡片双视图切换）：
| ☑ | 媒体名称 | 价格 | 出稿率 | 收录率 | 电脑权重 | 移动权重 | 备注 | 操作 |
- 出稿率/收录率：进度条形式显示
- 已选：显示已选媒体数量

底部栏（固定）：
- 可用余额：XX元 [去充值]
- 已选媒体数：N个
- 预计总价：XX元
- [清空已选] [下一步：确认投稿]

优化点：
1. 增加"媒体对比"功能（最多3个媒体并排对比各项指标）
2. 增加"智能推荐"按钮（根据行业和地区自动推荐高性价比媒体）
3. 价格改为可按总价/每篇排序
4. 增加媒体详情弹窗（历史成功案例/实际效果截图）
```

---

### 📈 PROMPT 9：代理端控制台（Dashboard）

```
帮我创建代理端控制台 views/agent/Dashboard.vue

布局：标准左右布局，左侧菜单已有，右侧主内容区

内容结构：

1. 顶部 Banner 区（轮播或固定）：
   - 品牌宣传标语 + 使用教程快速入口（两个link：代理后台操作教程/商户GEO操作教程）

2. 账户信息卡片（蓝紫渐变）：
   - 登录账号名
   - 到期时间
   - [客户端下载] 按钮（主要CTA）

3. 5步操作流程（横向箭头进度）：
   ①算力充值 → ②开户套餐 → ③开通商户 → ④网站配置 → ⑤矣岩认证
   每步可点击跳转，已完成步骤打勾高亮

4. 矣岩认证剩余次数（当前可用N次）

5. 核心数据统计（6个渐变色卡片，有背景装饰图案）：
   开通商户数 / 文章生成数 / 文章发布数 / 发布媒体数 / 集成网站数 / 自动策略数

6. 图表区（两栏）：
   左：平台总发布 - 环形饼图（媒体发布量 vs 网站发布量）
   右：文章发布频次 - 折线图，X轴日期，Y轴发布量，时间切换：7天/15天/30天

优化：
- 增加"本月收入"和"商户续费提醒"两个提示卡
- 图表增加空数据友好提示
```

---

### 📋 PROMPT 10：商户列表页

```
帮我创建商户列表页面 views/agent/merchant/MerchantList.vue

页面顶部：
- 标题"商家列表"
- 右侧：[添加商户] 按钮 / 筛选图标 / 刷新图标
- 信息栏：可用算力: XXXXX / 已用算力: XXXXX（醒目颜色）
- 提示：对商户进行封禁操作，无法再次解禁（需联系平台总管理员）

商户列表表格（支持多选）：
| ☑ | 商家信息 | 登录账号 | 算力 | 来源备注 | 时间 | 认证信息 | 操作 |

- 商家信息：头像(圆形) + ID + 名称 + 子账号授权数[修改]
- 登录账号：账号 + SUID + 状态(开启绿/封禁红 可点击切换)
- 算力：已用/剩余 + [分配算力] 按钮（弹出输入框）
- 时间：开通时间 / 活跃时间 / 到期时间（三行）
- 认证信息：已认证(绿)/未认证(红) + 可用认证次数 + [分配]
- 操作：[一键登录] [编辑商户] [存储配置]

一键登录：打开新标签页，带 token 直接进入该商户的商家后台

添加商户弹窗：
- 选择开户套餐（下拉，展示套餐详情）
- 来源备注
- 商家名称
- 手机号
- 自动生成随机密码（可修改）
- 子账号授权数（默认套餐设定值）

优化：
1. 到期时间前7天显示橙色警告，前1天显示红色
2. 支持批量导出商户数据（Excel）
3. 增加商户使用情况汇总（活跃度评分）
```

---

### 🔧 PROMPT 11：后端 API 设计

```
帮我设计并实现 GEO 平台的后端 API（Node.js + Express + TypeScript + Prisma + MySQL）

核心数据模型（Prisma Schema）：

model Agent {
  id          Int       @id @default(autoincrement())
  phone       String    @unique
  password    String
  companyName String
  totalPower  Int       @default(0)  // 总算力
  usedPower   Int       @default(0)  // 已用算力
  expireAt    DateTime
  createdAt   DateTime  @default(now())
  merchants   Merchant[]
  packages    Package[]
}

model Package {
  id          Int      @id @default(autoincrement())
  agentId     Int
  name        String
  power       Int      // 包含算力
  storage     Int      // 分配空间 GB
  validMonths Int      // 有效期（月）
  subAccounts Int      // 子账号数
  price       Decimal
  agent       Agent    @relation(fields: [agentId], references: [id])
}

model Merchant {
  id          Int      @id @default(autoincrement())
  agentId     Int
  phone       String   @unique
  suid        String   @unique
  shopName    String
  avatar      String?
  totalPower  Int      @default(1000)
  usedPower   Int      @default(0)
  storage     Int      @default(3)  // GB
  isActive    Boolean  @default(true)
  isCertified Boolean  @default(false)
  certQuota   Int      @default(0)
  sourceNote  String?
  expireAt    DateTime
  agent       Agent    @relation(fields: [agentId], references: [id])
  keywords    Keyword[]
  articles    Article[]
  tasks       Task[]
}

model Keyword {
  id         Int      @id @default(autoincrement())
  merchantId Int
  groupName  String
  word       String
  isPositive Boolean  @default(true)
  weight     Int      @default(0)
  isIndexed  Boolean  @default(false)
  indexedAt  DateTime?
  merchant   Merchant @relation(fields: [merchantId], references: [id])
}

model Article {
  id          Int      @id @default(autoincrement())
  merchantId  Int
  title       String
  content     String   @db.LongText
  platform    String
  status      String   // draft/published/failed
  publishedAt DateTime?
  powerCost   Int
  merchant    Merchant @relation(fields: [merchantId], references: [id])
}

model Task {
  id          Int      @id @default(autoincrement())
  merchantId  Int
  type        String   // auto_create/auto_publish/indexing
  status      String   // pending/running/paused/completed/failed
  config      Json
  progress    Int      @default(0)
  createdAt   DateTime @default(now())
  merchant    Merchant @relation(fields: [merchantId], references: [id])
}

请实现以下 API 路由：

// 认证
POST /api/auth/login-agent
POST /api/auth/login-merchant
POST /api/auth/agent-login-as-merchant  // 一键登录

// 代理端
GET  /api/agent/dashboard
GET  /api/agent/merchants
POST /api/agent/merchants
PUT  /api/agent/merchants/:id
POST /api/agent/merchants/:id/allocate-power
GET  /api/agent/packages
POST /api/agent/packages
GET  /api/agent/transactions

// 商家端
GET  /api/merchant/dashboard
GET  /api/merchant/keywords
POST /api/merchant/keywords
PUT  /api/merchant/keywords/:id
POST /api/merchant/keywords/batch-import
GET  /api/merchant/articles
POST /api/merchant/articles/create-with-ai  // 调用 LLM API
GET  /api/merchant/tasks
POST /api/merchant/tasks
PUT  /api/merchant/tasks/:id/status
GET  /api/merchant/tasks/:id/logs           // SSE 流式推送
```

---

## 七、开发优先级建议

```
Phase 1（核心 MVP，2-3周）：
✅ 统一登录 + 角色路由
✅ 代理端：控制台 + 商户管理 + 账务管理
✅ 商家端：工作台 + 蒸馏词库 + 知识库
✅ 商家端：一键创作（调用 LLM API）

Phase 2（核心功能，2-3周）：
⬜ 商家端：自动创作（云端队列）
⬜ 商家端：媒体账号管理 + 自动化发布
⬜ 商家端：收录任务（云端版）
⬜ 商家端：收录大模型授权

Phase 3（增值功能，2周）：
⬜ 付费投稿媒体库
⬜ 数据效果看板（GEO收录趋势）
⬜ 合伙人管理
⬜ 移动端查看页面

Phase 4（体验优化，持续）：
⬜ 消息通知系统
⬜ 商家使用引导优化
⬜ 批量操作优化
⬜ 数据导出
```

---

## 八、差异化建议（你的版本要比原系统好在哪）

1. **云端调度** — 不依赖本地浏览器，所有任务在服务器执行
2. **效果看板** — 增加GEO收录率趋势、AI平台曝光量、品牌词出现频率
3. **算力透明化** — 每步操作显示预计消耗算力，有余量预警
4. **移动端支持** — 至少 Dashboard 支持手机查看
5. **智能推荐** — 付费投稿时根据行业+地区推荐高性价比媒体
6. **统一登录** — 不再分两个URL，一个入口根据角色自动跳转
7. **AI平台收录监控** — 自动定期检测各AI平台的品牌词收录状态
8. **文章质量评分** — AI创作完成后给出原创度/合规性/SEO分数
